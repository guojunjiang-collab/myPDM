# myPDM 项目进展总结 - 2026-04-12

## 📅 日期范围
2026-04-10 10:40 至 2026-04-12 23:51（GMT+8）

## 📋 核心修改文件
- **前端文件**：`D:\OpenCode\myPDM\frontend\index.html`
- **后端模型**：`backend/app/models.py`
- **后端接口**：`backend/app/schemas.py`、`backend/app/crud.py`、`backend/app/routers/parts.py`
- **数据库**：PostgreSQL 表结构更新

---

## 🚀 主要功能实现

### 1. 字典系统清理（4月10日-12日）
**目标**：移除冗余字典字段，简化数据结构

**清理范围**：
| 字典类型 | 清理内容 | 状态 |
|---------|---------|------|
| dict_materials | 保留（零件材质） | ✅ 保留 |
| dict_units | 完全移除 | ✅ 已清理 |
| dict_suppliers | 完全移除 | ✅ 已清理 |
| dict_products | 完全移除 | ✅ 已清理 |

**前端清理**：
- `SyncModal`：同步实体列表从7类缩减为3类（parts/components/users）
- `_editPart`/`_viewPart`：移除材质、计量单位字段
- `_editComp`/`_viewComp`：移除所属产品字段
- `settings` 页面：完全移除字典管理UI
- `Store._fieldMap`：移除 material/unit/supplier/product 映射

**后端清理**：
- `models.py`：`Part` 模型移除 `material`/`unit`/`supplier`；`Assembly` 模型移除 `product`
- `schemas.py`：对应 Pydantic 字段移除
- `crud.py`：BOM tree 详情中移除 `unit` 字段
- `dict.py`：`DICT_TYPES` 从4种缩减为1种
- `init.sql`：删除对应表和列

---

### 2. 修订记录功能（4月12日）
**目标**：记录零部件当前版本内的每次字段修改明细

**设计原则**：
- **修订记录（revisions）**：记录当前版本内所有字段修改的详细变更
- **版本历史（versionHistory）**：已移除，仅保留修订记录

**追踪字段**（零件/部件相同）：
- `code`（件号）、`name`（名称）、`spec`（规格型号）
- `status`（状态）、`remark`（备注）
- `sourceFile`（源文件）、`drawing`（图纸）、`stp`（STP）、`pdf`（PDF）

**数据结构**：
```javascript
revisions: [{
  date: 1712937600000,           // 修改时间戳
  author: "张工程师",             // 修改人
  changes: [
    { field: "名称", oldVal: "M12螺栓", newVal: "M12螺栓-加长" },
    { field: "规格型号", oldVal: "M12×50", newVal: "M12×80" }
  ]
}]
```

**记录时机**：
| 场景 | 记录内容 |
|------|---------|
| 编辑保存 | 对比9个字段，仅记录变化的字段 |
| 新增 | 初始化 `revisions: []` |
| 升版 | 记录 `changes: [{field: '版本', oldVal: 'A', newVal: 'B'}]` |

**UI展示**：
- `_viewPart`/`_viewComp` 底部始终显示「📝 修订记录」区域
- 有记录：按时间倒序，显示修改人、时间、字段变更对比（红色旧值→绿色新值）
- 无记录：灰色提示「暂无修订记录」

**后端支持**：
- `Part.revisions` → JSONB 列，默认 `[]`
- `Assembly.revisions` → JSONB 列，默认 `[]`

---

### 3. 清除缓存功能（4月12日）
**目标**：提供一键清除本地缓存，保留登录状态

**功能位置**：顶部工具栏，同步数据按钮右侧

**实现逻辑**：
```javascript
// 保留的键
keepKeys = ['bom_api_token', 'bom_current_user']

// 清除的键
// 所有 bom_ 开头，但不在 keepKeys 中的 localStorage 项
// 包括：bom_parts, bom_components, bom_users, bom_logs 等

// 重置状态
Store._syncQueue = []
Store._syncRunning = false
Store._syncStatus = 'idle'
Store._syncErrorCount = 0
```

**Bug修复**：
- 原代码错误使用 `bom_user`，实际键名为 `bom_current_user`
- 修复后清除缓存不再误删用户信息，无需重新登录

---

### 4. 同步字段映射修复（4月12日）
**问题**：同步后零件详情未显示规格型号等数据

**根因**：`Store._fieldMap.parts` 缺少 `createdAt`/`updatedAt` 映射

**修复**：
```javascript
// 修改前
parts: { remark: 'remark', lossRate: 'loss_rate', revisions: 'revisions' }

// 修改后  
parts: { 
  remark: 'remark', 
  lossRate: 'loss_rate', 
  revisions: 'revisions',
  createdAt: 'created_at', 
  updatedAt: 'updated_at' 
}
```

---

### 5. 升版同步Bug修复（4月12日）
**问题**：升版零件后同步数据报错，新零件上传失败

**根因**：后端 `parts.code` 有唯一约束，但前端升版设计是**同编码多版本**（A→B→C）

**修复内容**：

| 文件 | 改动 |
|------|------|
| `models.py` | 添加 `version` 字段，改为 `(code, version)` 联合唯一约束 |
| `schemas.py` | `PartBase`/`PartCreate`/`PartUpdate` 添加 `version` 字段 |
| `crud.py` | `get_part_by_code(db, code, version=None)` 支持按版本查询 |
| `parts.py` | 创建零件时检查 `(code, version)` 联合唯一 |
| 数据库 | 添加 `version` 列，删除旧唯一约束，添加联合唯一约束 |

**SQL变更**：
```sql
ALTER TABLE parts ADD COLUMN version VARCHAR(32) DEFAULT 'A';
ALTER TABLE parts DROP CONSTRAINT parts_code_key;
ALTER TABLE parts ADD CONSTRAINT uix_part_code_version UNIQUE (code, version);
```

---

## 🐛 关键Bug修复汇总

| Bug | 严重度 | 状态 | 修复位置 |
|-----|--------|------|---------|
| 清除缓存误删用户信息 | 高 | ✅ 已修复 | `SyncModal.init` 中 `keepKeys` 修正为 `bom_current_user` |
| 升版零件同步失败 | 高 | ✅ 已修复 | 后端改为 `(code, version)` 联合唯一 |
| 同步后字段不显示 | 中 | ✅ 已修复 | `_fieldMap.parts` 补充 `createdAt`/`updatedAt` |
| API._fetch 错误无 status | 高 | ✅ 已修复 | 错误对象添加 `err.status = r.status` |
| 后台队列部件子项ID错误 | 高 | ⏳ 待确认 | `_execSync` add 分支中 `record.id` 更新顺序 |

---

## 📊 技术架构更新

### 前端架构
- **同步机制**：手动全量同步 + 后台实时队列双轨并行
- **字段映射**：`Store._fieldMap` 统一管理前后端字段名转换
- **权限控制**：`Auth.canEdit()` / `Auth.isAdmin()` 统一入口
- **状态管理**：`draft`/`frozen`/`released`/`obsolete` 四态工作流

### 后端架构
- **模型关系**：
  ```
  Part (code, version) 联合唯一
  Assembly (code, version) 联合唯一  
  BOMItem → part_id / assembly_id
  ```
- **API规范**：RESTful + snake_case 字段名
- **数据库**：PostgreSQL + JSONB 存储修订记录

---

## 📈 数据流说明

### 同步数据流程
```
用户点击同步
    ↓
SyncModal.start()
    ↓
┌─────────────────────────┐
│ 第一阶段：拉取（5-30%）   │
│ 遍历3类数据：             │
│ parts / components / users│
│   ↓                      │
│ API.getXxx() 拉取全量     │
│   ↓                      │
│ reverseMap 字段转换       │
│ （snake_case→camelCase）  │
│   ↓                      │
│ Upsert 本地 Store        │
└─────────────────────────┘
    ↓
┌─────────────────────────┐
│ 第二阶段：上传（40-95%） │
│ 找出「本地有服务器无」     │
│   ↓                      │
│ fmap 字段转换             │
│ （camelCase→snake_case）  │
│   ↓                      │
│ 分类上传                  │
│ · 部件：主体→子项        │
│ · 其他：直接上传          │
└─────────────────────────┘
```

### 后台实时队列
```
用户操作 → Store._enqueue() → _syncQueue[] 
    ↓
_processQueue() → _execSync(task)
    ↓
┌────────────────────────────────────────┐
│ add: toApi() → API.create →            │
│     _updateLocalId 替换临时ID →        │
│     部件子项关联                        │
├────────────────────────────────────────┤
│ update: toApi() → API.update →         │
│     部件：先主体后子项                  │
├────────────────────────────────────────┤
│ delete: API.delete(id) →               │
│     部件：先删子项再删主体              │
└────────────────────────────────────────┘
```

---

## ⚠️ 已知问题与待办

1. **后台队列 `_execSync` 部件子项ID问题**
   - 状态：待确认（代码中 `_updateLocalId` 在 parts 循环之前，理论上已修复）
   - 影响：后台队列创建部件时，子项可能关联到错误的临时ID

2. **`_fieldMap.parts` 缺少 `partId→part_id` 映射**
   - 状态：待修复
   - 影响：部件子项上传时字段名可能不正确

3. **部件子项字段名不统一**
   - `partId` / `child_id` / `childId` 混用
   - 后端期望 `child_id`

4. **同步队列残留问题**
   - 本地 delete 操作入队后，同记录的 add/update 旧操作可能残留

---

## 👥 演示账号状态

| 账号 | 密码 | 角色 | 权限说明 |
|------|------|------|----------|
| admin | 123456 | 管理员 | 全权限，可编辑所有状态 |
| engineer | 123456 | 工程师 | 可编辑草稿/冻结状态，可升版 |
| production | 123456 | 生产管理 | 仅查看权限 |
| guest | 123456 | 访客 | 仅查看权限 |

---

## 📝 版本状态

**myPDM v1.0.0**（后端联调完成版）

- ✅ 前后端数据同步
- ✅ 用户认证与权限
- ✅ 零部件CRUD
- ✅ 版本管理（升版）
- ✅ 修订记录
- ✅ BOM层级管理
- ✅ 操作日志

---

**总结人**：小Q（QClaw 智能秘书）  
**总结时间**：2026-04-12 23:55 GMT+8  
**文件位置**：`D:\OpenCode\myPDM\项目说明\每日总结-2026-04-12.md`
