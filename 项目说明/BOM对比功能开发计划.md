# BOM版本对比功能开发计划

**文档版本：** v1.3  
**制定日期：** 2026年4月20日  
**最后更新：** 2026年4月21日（根据开发进展更新）  
**制定人：** 国军  
**依据文档：** 《BOM版本对比功能规划文档.docx》

## 项目背景
现有BOM工具已具备基础的BOM对比功能，但对比《BOM版本对比功能规划文档》的要求，在可视化对比、自动变更识别、层级树对比、操作效率等方面存在较大差距。本项目旨在按照规划文档的规范，全面升级BOM对比功能，提供专业、直观、高效的版本差异分析工具。

---

## 📈 进展状态（截至2026年4月21日）

| 阶段 | 状态 | 完成日期 | 实际输出物 |
|------|------|----------|------------|
| 第一阶段：需求分析与技术设计 | ✅ 已完成 | 2026-04-20 | 详细设计文档、API设计、前端组件结构 |
| 第二阶段：后端对比算法API实现 | ✅ 已完成 | 2026-04-20 | `/api/bom/compare` 接口、对比算法模块、数据模型扩展 |
| 第三阶段：前端双栏对比界面开发 | ✅ 已完成 | 2026-04-20 | 新版BOM对比页面、API集成（含认证修复）、对比表格渲染、交互功能（展开/折叠、只显示变更）、CSS样式扩展 |
| 第四阶段：操作功能与导出实现 | 🟡 进行中 | 2026-04-20 | CSV导出功能、工具栏优化（全展开/全折叠、只显示变更）、差异对比显示功能实现 |
| 第五阶段：集成测试与优化 | ⭕ 未开始 | - | - |

**第二阶段实现详情：**
- ✅ **对比算法模块**：`backend/app/bom/compare.py` (10,901字节)
- ✅ **数据模型扩展**：4个对比相关Pydantic模型（`BOMCompareOptions`、`BOMCompareRequest`、`BOMCompareNode`、`BOMCompareResponse`）
- ✅ **API接口**：`POST /api/bom/compare` 端点，支持角色认证（admin/engineer/production）
- ✅ **数据库优化**：推荐索引 `bom_items(parent_type, parent_id)` 和 `bom_items(child_type, child_id)`
- ✅ **服务部署**：已重启 `bom_backend` 容器，API生效

**当前工作重点：** 
- ✅ 解决“没有列出对比差异”问题（已实现完整的变更类型映射与视觉规范）
- ⭕ 实现变更定位功能（上一个/下一个变更按钮）
- ⭕ 扩展导出格式（Excel/PDF）
- ⭕ 集成测试与性能优化

---

## 📋 开发计划总览

| 阶段 | 主要任务 | 预估工时 | 输出物 |
|------|----------|----------|--------|
| **第一阶段** | 需求分析与技术设计 | 0.5天 | 详细API设计、前端组件结构、算法流程图 |
| **第二阶段** | 后端对比算法API实现 | 1.5天 | `/api/bom/compare` 接口、测试用例 |
| **第三阶段** | 前端双栏对比界面开发 | 2天 | 新版BOM对比页面（支持展开/折叠、高亮、过滤） |
| **第四阶段** | 操作功能与导出实现 | 1天 | 全展/全收、只显变更、定位、Excel/PDF导出 |
| **第五阶段** | 集成测试与优化 | 1天 | 完整测试报告、性能优化、用户体验调整 |
| **总计** | **5个工作日** | **6天** | 可交付的完整BOM版本对比功能 |

---

## 🎯 第一阶段：需求分析与技术设计（0.5天）

### 1.1 文档深度解读
- 逐条分析规划文档中的功能点、视觉规范、操作流程
- 识别与现有BOM工具的差异点（当前对比功能可能较简单）

### 1.2 技术方案设计
- **API设计**：
  ```python
  POST /api/bom/compare
  {
    "left_assembly_id": "uuid",  # 左侧装配体版本ID
    "right_assembly_id": "uuid", # 右侧装配体版本ID
    "options": {
      "ignore_quantity": false,  # 是否忽略数量差异
      "max_depth": 10            # 最大对比深度
    }
  }
  ```
  响应结构包含变更分类（增、删、改、内部变更）、层级信息、差异详情。

- **前端组件结构**：
  - `BomComparePage`：主页面，管理状态
  - `VersionSelector`：左右版本选择器
  - `CompareTable`：双栏对比表格（支持虚拟滚动）
  - `DiffHighlighter`：差异高亮组件
  - `OperationToolbar`：操作工具栏

- **对比算法流程**：
  1. 预处理：拉取两个版本的BOM树，扁平化为带层级标记的列表
  2. 节点匹配：基于“层级+排序号+物料号”作为唯一键进行左右匹配
  3. 变更识别：按文档定义的4类变更（无、增、删、改、内部变更）分类
  4. 层级树重建：构建可展开/折叠的对比树结构

### 1.3 产出文档
- `BOM对比功能详细设计说明书.md`
- `API接口文档.md`
- `前端组件结构图`

---

## 🛠 第二阶段：后端对比算法API实现（1.5天）

### 2.1 数据库查询优化
- 编写递归CTE查询，快速获取装配体的完整BOM树（含层级、排序号）
- 添加索引优化：`bom_items(assembly_id, assembly_version)`

### 2.2 核心算法实现
- **预处理模块**：`bom/flatten.py`
  - 输入：装配体ID + 版本号
  - 输出：扁平化列表，每项包含 `level`, `sort`, `part_code`, `part_name`, `quantity`, `version` 等

- **匹配与对比模块**：`bom/compare.py`
  - 实现文档3.2节的变更识别逻辑
  - 内部变更检测：子项数量或顺序变化视为内部变更

- **API路由**：`routers/bom.py` 新增 `/compare` 端点
  - 输入验证（Pydantic Schema）
  - 调用对比算法，返回结构化差异数据
  - 错误处理与日志记录

### 2.3 测试与验证
- 单元测试：覆盖4种典型场景（文档第8章）
- 集成测试：模拟真实BOM数据，验证算法准确性
- 性能测试：千级节点BOM的对比响应时间 < 2s

### 2.4 产出物
- 可调用的 `/api/bom/compare` API
- 算法测试报告
- 性能基准数据

---

## 🎨 第三阶段：前端双栏对比界面开发（2天）

### 3.1 页面结构重构
- 修改 `BOM对比` 选项卡，替换现有简单对比界面
- 采用三栏布局（左版本选择 + 中操作栏 + 右版本选择）
- 下方为主对比区域（双栏表格 + 中缝提示）

### 3.2 对比表格组件
- **双栏同步滚动**：使用 `scroll` 事件同步左右滚动位置
- **层级缩进**：CSS `padding-left: calc(level * 2em)` 实现视觉层级
- **行高亮**：根据变更类型应用不同背景色
  - 无差异：白色
  - 新增：浅绿色 `#E6F4EA`，前缀 `+`
  - 删除：浅红色 `#FDEDED`，前缀 `-`
  - 修改：浅黄色 `#FFFBEB`，前缀 `✏️`
  - 内部变更：浅橙色 `#FFF3E0`，前缀 `?`

### 3.3 展开/折叠功能
- 每行左侧添加展开/折叠图标（▶/▼）
- 折叠时只显示父项，统计子项变更数量
- 状态持久化（localStorage记忆用户展开状态）

### 3.4 实时过滤
- 实现“只显示变更”开关
- 过滤逻辑：隐藏所有“无差异”行，保持层级关系

### 3.5 产出物
- 完整的双栏对比界面
- 响应式设计（支持不同屏幕宽度）
- 基础交互功能（展开/折叠、高亮、过滤）

---

## ⚙️ 第四阶段：操作功能与导出实现（1天）

### 4.1 操作工具栏（当前实现状态）
- ✅ **全展开/全折叠**：已实现，支持一键控制所有层级
- ✅ **差异对比显示功能**：已实现完整的变更类型映射（add/delete/modify/none/internal → 新增/删除/修改/未变/内部变更），统一变更类型文本显示，补充CSS样式（.internal-row），严格遵循视觉规范
- ⭕ **变更定位**：
  - 上一个变更 / 下一个变更 按钮（待实现）
  - 滚动到对应行并高亮闪烁（待实现）
- ⭕ **版本切换**：下拉菜单快速切换对比版本（待实现）
- ⭕ **刷新对比**：重新运行对比算法（待实现）

### 4.2 导出功能（当前实现状态）
- ✅ **CSV导出**：已实现，生成带BOM的UTF-8 CSV文件，Excel可直接打开无乱码
  - 导出文件命名：`BOM对比_YYYY-MM-DD.csv`
  - 包含完整对比数据（层级、变更类型、左右版本详细信息）
- ⭕ **Excel导出**：使用 `xlsx` 库生成带格式的对比报告（待实现）
  - 左右版本并排显示
  - 差异单元格着色
  - 包含汇总统计页
- ⭕ **PDF导出**：使用 `jsPDF` + `html2canvas` 生成可打印报告（待实现）
- 导出文件命名：`BOM对比_左版本_右版本_时间戳.xlsx`（待统一）

### 4.3 用户偏好保存（待实现）
- ⭕ 记住用户最后对比的版本对
- ⭕ 保存列宽、排序等界面状态
- ⭕ 导出格式默认选择

### 4.4 产出物（当前）
- ✅ 操作工具栏基础功能（全展开/全折叠、只显示变更）
- ✅ CSV导出功能
- ✅ 差异对比显示功能（完整的变更类型映射与视觉规范实现）
- ⭕ 变更定位功能（待实现）
- ⭕ Excel/PDF导出功能（待实现）
- ⭕ 用户偏好持久化（待实现）

---

## 🧪 第五阶段：集成测试与优化（1天）

### 5.1 端到端测试
- 创建测试数据集（4种典型场景 + 边界案例）
- 验证前后端数据流一致性
- 测试大BOM（1000+节点）的性能与内存使用

### 5.2 用户体验优化
- 加载状态提示（骨架屏）
- 空状态处理（无差异时的友好提示）
- 错误边界处理（网络错误、数据异常）
- 键盘快捷键支持（←→切换变更，空格展开/折叠）

### 5.3 性能优化
- 虚拟滚动：只渲染可视区域的行
- 防抖处理：版本切换时延迟对比计算
- 缓存策略：相同版本的对比结果缓存5分钟

### 5.4 文档与培训
- 更新用户手册，新增“BOM对比功能使用指南”
- 录制功能演示视频（可选）
- 团队内部技术分享（对比算法实现原理）

### 5.5 产出物
- 测试报告与性能数据
- 优化后的生产级功能
- 用户文档与培训材料

---

## 📊 资源与风险

### 所需资源
1. **开发人员**：1名全栈开发（熟悉FastAPI + 前端JS）
2. **测试数据**：真实BOM数据用于测试
3. **工具**：Chrome DevTools、Postman、Excel

### 风险与应对
| 风险 | 可能性 | 影响 | 缓解措施 |
|------|--------|------|----------|
| 大BOM性能问题 | 中 | 高 | 实现虚拟滚动、分页对比、后端分块处理 |
| 算法复杂度高 | 中 | 中 | 先实现简化版本（仅一级对比），再迭代增加层级 |
| 前端双栏同步难度 | 低 | 中 | 使用现有库（如`react-split-pane`借鉴思路） |
| 导出功能兼容性 | 低 | 低 | 提供Excel + CSV双格式导出 |

---

## 🚀 立即行动建议

### 今日可启动任务
1. **查看现有对比功能**：运行BOM工具，进入“BOM对比”选项卡，了解当前实现程度
2. **确认技术细节**：
   - 装配体版本数据格式（`assembly_version`字段）
   - 当前BOM树获取API（`/api/assemblies/{id}/parts`）
   - 现有对比前端的代码位置（`pages.js`中的`BomCompare`相关函数）
3. **创建详细设计文档**：基于本计划细化每个模块的实现细节

### 开发环境准备
```bash
# 1. 启动Docker环境
cd "D:\OpenCode\myPDM"
docker-compose up -d

# 2. 验证后端运行
curl http://localhost/api/docs

# 3. 准备测试数据
# 创建两个版本的装配体，包含增、删、改、内部变更场景
```

---

**文档变更记录**
| 版本 | 日期 | 修改说明 | 修改人 |
|------|------|----------|--------|
| v1.3 | 2026-04-21 | 更新差异对比显示功能实现，更新当前工作重点与产出物，标记差异显示问题已解决 | 国军 |
| v1.2 | 2026-04-20 | 更新第三阶段完成状态，第四阶段进行中，添加CSV导出功能，更新当前工作重点 | 国军 |
| v1.1 | 2026-04-20 | 更新第三阶段状态，标记为已启动，更新当前工作重点 | 国军 |
| v1.0 | 2026-04-20 | 初版，基于规划文档制定完整开发计划 | 国军 |

**相关文档**
- [BOM版本对比功能规划文档.docx](./BOM版本对比功能规划文档.docx)
- [项目说明.md](./项目说明.md)
- [自定义字段功能分析.md](./自定义字段功能分析.md)