# 压缩包内容预览 —— 设计方案

> **状态**: 待开发  
> **创建**: 2026-05-09  
> **目标**: 实现 ZIP / TAR 压缩包的内容预览，展示内部文件夹层级结构

---

## 一、需求概述

在附件管理中，用户上传压缩包（ZIP / TAR / TAR.GZ）后，点击"预览"应能看到压缩包内部的文件夹层级和文件列表，无需下载或解压。

## 二、现状分析

| 项目 | 当前状态 |
|------|----------|
| **预览文件类型** | 仅 PDF（浏览器内嵌渲染）和 STP（转 glTF 三维预览） |
| **非支持格式** | 弹窗 `alert('该格式暂不支持预览')` |
| **预览入口** | `DocumentDetailContent.tsx:68` / `EntityDocumentSection.tsx:162` |
| **预览后端** | `GET /api/v2/attachments/{id}/preview?token=` → `Content-Disposition: inline` |
| **依赖库** | 无压缩库，Python stdlib 自带 `zipfile` / `tarfile` |
| **现有压缩代码** | 无，全项目无任何 `zipfile`/`tarfile`/`rarfile` 引用 |

**预览触发代码**（`DocumentDetailContent.tsx:68-80`）：

```typescript
const handlePreview = (attId: string, fileName: string) => {
  const ext = fileName.split('.').pop()?.toLowerCase() || '';
  if (ext !== 'pdf') {
    alert('该格式暂不支持预览');
    return;
  }
  const token = useAuthStore.getState().token;
  window.open(`/api/v2/attachments/${attId}/preview?token=${encodeURIComponent(token)}`, '_blank');
};
```

## 三、格式支持

| 格式 | 扩展名 | Python 库 | 需新增 pip 依赖？ | 优先级 |
|------|--------|-----------|------------------|--------|
| **ZIP** | `.zip` | `zipfile`（标准库） | ❌ 不需要 | P0 — 首期实现 |
| **TAR** | `.tar` | `tarfile`（标准库） | ❌ 不需要 | P0 — 首期实现 |
| **TAR.GZ** | `.tar.gz` `.tgz` | `tarfile`（标准库） | ❌ 不需要 | P0 — 首期实现 |
| **RAR** | `.rar` | `rarfile` | ⚠️ 需 `pip install rarfile` | P1 — 后续扩展 |
| **7Z** | `.7z` | `py7zr` | ⚠️ 需 `pip install py7zr` | P1 — 后续扩展 |

**决定**：首期仅实现 ZIP + TAR/TAR.GZ，零新增依赖。RAR/7Z 后续按需扩展。

## 四、架构设计

```
┌──────────────────────────────────────────────────────────┐
│                     前端 (React + TS)                     │
│                                                          │
│  handlePreview(attId, fileName)                          │
│    │                                                     │
│    ├── ext === 'pdf'  ──→ window.open(/preview)  (现有)  │
│    │                                                     │
│    ├── ext ∈ {zip,tar,gz,tgz}                            │
│    │     ──→ fetch GET /archive-tree?token=              │
│    │     ──→ <ArchiveTreeModal> 展示树形结构            │
│    │                                                     │
│    └── 其他 ──→ alert("该格式暂不支持预览")              │
│                                                          │
├──────────────────────────────────────────────────────────┤
│                     后端 (FastAPI)                        │
│                                                          │
│  GET /api/v2/attachments/{id}/archive-tree?token={jwt}   │
│    │                                                     │
│    ├── JWT 认证 (复用现有 preview 端点的验证逻辑)         │
│    ├── 从数据库获取附件记录 (file_name, file_path)       │
│    ├── 解析文件扩展名 → 选择读取器                       │
│    ├── zipfile.ZipFile() / tarfile.open() 列目录        │
│    └── 构建 JSON 树结构 → 返回                           │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

## 五、API 设计

### `GET /api/v2/attachments/{attachment_id}/archive-tree`

**查询参数**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `token` | string | 是 | JWT 认证令牌 |

**权限**：`production` 及以上可预览，`guest` 不可预览（与现有 `/preview` 端点一致）。

**成功响应** `200 OK`：

```json
{
  "file_name": "设计图纸.zip",
  "total_files": 142,
  "total_size": 10485760,
  "tree": [
    {
      "name": "机械部分/",
      "type": "dir",
      "size": 0,
      "children": [
        {
          "name": "主轴.dwg",
          "type": "file",
          "size": 2048000,
          "compressed_size": 983040
        },
        {
          "name": "齿轮.pdf",
          "type": "file",
          "size": 512000,
          "compressed_size": 498000
        }
      ]
    },
    {
      "name": "电气部分/",
      "type": "dir",
      "size": 0,
      "children": [
        {
          "name": "电路图.pdf",
          "type": "file",
          "size": 380000,
          "compressed_size": 375000
        },
        {
          "name": "BOM.xlsx",
          "type": "file",
          "size": 128000,
          "compressed_size": 64000
        }
      ]
    },
    {
      "name": "总装图.stp",
      "type": "file",
      "size": 4500000,
      "compressed_size": 2100000
    }
  ]
}
```

**错误响应**：

| 状态码 | 说明 |
|--------|------|
| `401` | Token 缺失或无效 |
| `404` | 附件 / 文件不存在 |
| `400` | 不支持的压缩格式 |
| `500` | 压缩包损坏或读取失败 |

## 六、后端实现

### 6.1 文件结构

```
backend/app/
├── bom/
│   └── archive_reader.py    ← 新建：压缩包读取工具
└── routers/
    └── attachments_v2.py    ← 修改：新增 /archive-tree 端点
```

### 6.2 `archive_reader.py` — 压缩包读取工具

```python
"""
压缩包内容读取工具

支持格式: ZIP (.zip), TAR (.tar, .tar.gz, .tgz)
依赖: 仅 Python 标准库 (zipfile, tarfile)
"""

import zipfile
import tarfile
from pathlib import Path
from typing import Dict, List, Any


# 支持的扩展名
SUPPORTED_EXTENSIONS = {'.zip', '.tar', '.gz', '.tgz'}


def read_archive_tree(file_path: str) -> Dict[str, Any]:
    """
    读取压缩包内容，返回嵌套树结构
    
    Args:
        file_path: 压缩包文件路径
        
    Returns:
        { tree, total_files, total_size }
        
    Raises:
        ValueError: 不支持的格式
        zipfile.BadZipFile: ZIP 文件损坏
        tarfile.ReadError: TAR 文件损坏
    """
    ext = Path(file_path).suffix.lower()
    
    if ext == '.zip' or (ext == '.gz' and Path(file_path).suffixes[-2:] == ['.tar', '.gz']):
        return _read_zip(file_path) if ext == '.zip' else _read_tar(file_path)
    elif ext in ('.tar', '.gz', '.tgz'):
        return _read_tar(file_path)
    else:
        raise ValueError(f"不支持的压缩格式: {ext}")


def _read_zip(path: str) -> Dict[str, Any]:
    """读取 ZIP 文件内容"""
    entries = []
    with zipfile.ZipFile(path, 'r') as zf:
        for info in zf.infolist():
            entries.append({
                "path": info.filename.rstrip('/'),
                "type": "dir" if info.is_dir() else "file",
                "size": info.file_size,
                "compressed_size": info.compress_size,
            })
    return _build_tree(entries)


def _read_tar(path: str) -> Dict[str, Any]:
    """读取 TAR / TAR.GZ 文件内容"""
    entries = []
    with tarfile.open(path, 'r:*') as tf:  # 'r:*' 自动检测压缩类型
        for member in tf.getmembers():
            entries.append({
                "path": member.name.rstrip('/'),
                "type": "dir" if member.isdir() else "file",
                "size": member.size,
            })
    return _build_tree(entries)


def _build_tree(entries: List[dict]) -> Dict[str, Any]:
    """
    将扁平路径列表转为嵌套树结构
    
    算法:
    1. 按路径排序
    2. 逐层创建目录节点，维护 dir_map[路径前缀] → 节点的映射
    3. 文件节点挂到对应目录下
    """
    tree: List[dict] = []
    dir_map: Dict[str, dict] = {}
    
    # 按路径排序，确保父目录在子文件前
    sorted_entries = sorted(entries, key=lambda e: e["path"])
    
    for entry in sorted_entries:
        parts = entry["path"].split('/')
        
        # 构建父目录链
        for depth in range(len(parts) - 1):
            prefix = '/'.join(parts[:depth + 1])
            if prefix not in dir_map:
                node = {
                    "name": parts[depth],
                    "type": "dir",
                    "size": 0,
                    "children": []
                }
                dir_map[prefix] = node
                # 挂到上一级目录
                if depth == 0:
                    tree.append(node)
                else:
                    parent_key = '/'.join(parts[:depth])
                    dir_map[parent_key]["children"].append(node)
        
        # 添加文件节点
        if entry["type"] == "file":
            filename = parts[-1]
            node = {
                "name": filename,
                "type": "file",
                "size": entry["size"]
            }
            if "compressed_size" in entry:
                node["compressed_size"] = entry["compressed_size"]
            
            if len(parts) == 1:
                tree.append(node)  # 根级文件
            else:
                parent_key = '/'.join(parts[:-1])
                if parent_key in dir_map:
                    dir_map[parent_key]["children"].append(node)
                else:
                    # 防御：父目录不存在时挂到根
                    tree.append(node)
    
    total_files = sum(1 for e in entries if e["type"] == "file")
    total_size = sum(e["size"] for e in entries)
    
    return {
        "tree": tree,
        "total_files": total_files,
        "total_size": total_size
    }
```

### 6.3 `attachments_v2.py` — 新增端点

```python
from ..bom.archive_reader import read_archive_tree, SUPPORTED_EXTENSIONS


@router.get("/{attachment_id}/archive-tree")
async def get_archive_tree(
    attachment_id: uuid.UUID,
    token: str = None,
    db: Session = Depends(get_db)
):
    """
    获取压缩包内容树（ZIP / TAR / TAR.GZ）
    
    Args:
        attachment_id: 附件 UUID
        token: JWT 认证令牌（query 参数，方便浏览器直接访问）
        
    Returns:
        { file_name, tree, total_files, total_size }
    """
    # ---- JWT 认证 ----
    from jose import JWTError, jwt
    from .auth import SECRET_KEY, ALGORITHM
    
    if not token:
        raise HTTPException(status_code=401, detail="缺少认证令牌")
    
    try:
        payload = jwt.decode(token, SECRET_KEY, algorithms=[ALGORITHM])
        username = payload.get("sub")
        user_role = payload.get("role", "")
        if not username:
            raise HTTPException(status_code=401, detail="无效的认证令牌")
    except JWTError:
        raise HTTPException(status_code=401, detail="认证令牌验证失败")
    
    # 权限检查（与 /preview 端点一致）
    if user_role == "guest":
        raise HTTPException(status_code=403, detail="访客无预览权限")
    
    # ---- 获取附件 ----
    att = db.query(DocumentAttachment).filter(
        DocumentAttachment.id == attachment_id
    ).first()
    if not att:
        raise HTTPException(status_code=404, detail="附件不存在")

    # 检查格式
    ext = Path(att.file_name).suffix.lower()
    if ext not in SUPPORTED_EXTENSIONS:
        raise HTTPException(
            status_code=400,
            detail=f"不支持的压缩格式: {ext}，仅支持 ZIP / TAR / TAR.GZ"
        )
    
    # 定位文件
    file_path = None
    if hasattr(att, 'file_path') and att.file_path:
        full_path = file_storage.base_dir / att.file_path
        if full_path.exists():
            file_path = full_path
    
    if not file_path:
        raise HTTPException(status_code=404, detail="文件不存在")
    
    # ---- 读取压缩包 ----
    try:
        result = read_archive_tree(str(file_path))
    except (zipfile.BadZipFile, tarfile.ReadError) as e:
        raise HTTPException(status_code=500, detail=f"压缩包读取失败: {str(e)}")
    except ValueError as e:
        raise HTTPException(status_code=400, detail=str(e))
    
    return {
        "file_name": att.file_name,
        **result
    }
```

**需要新增的 import**（`attachments_v2.py` 顶部）：

```python
import zipfile
import tarfile
from pathlib import Path
from ..bom.archive_reader import read_archive_tree, SUPPORTED_EXTENSIONS
```

## 七、前端实现

### 7.1 文件结构

```
frontend/src/
├── components/
│   ├── ArchiveTreeModal.tsx       ← 新建：压缩包树形预览弹窗
│   ├── DocumentDetailContent.tsx  ← 修改：handlePreview 扩展
│   └── EntityDocumentSection.tsx  ← 修改：handlePreviewAttachment 扩展
├── services/
│   └── api.ts                     ← 修改：新增 attachmentsApi.archiveTree()
└── types/
    └── index.ts                   ← 修改：新增 ArchiveTreeResponse 类型
```

### 7.2 类型定义 — `types/index.ts`

新增：

```typescript
/** 压缩包内容树节点 */
export interface ArchiveTreeNode {
  name: string;
  type: 'file' | 'dir';
  size: number;
  compressed_size?: number;
  children?: ArchiveTreeNode[];
}

/** 压缩包内容树响应 */
export interface ArchiveTreeResponse {
  file_name: string;
  total_files: number;
  total_size: number;
  tree: ArchiveTreeNode[];
}
```

### 7.3 API 客户端 — `services/api.ts`

新增：

```typescript
// 附件 API
export const attachmentsApi = {
  // ... 现有方法 ...
  
  /** 获取压缩包内容树 */
  archiveTree: (attachmentId: string, token: string) =>
    api.get<ArchiveTreeResponse>(
      `/v2/attachments/${attachmentId}/archive-tree`,
      { params: { token } }
    ),
};
```

### 7.4 预览入口修改

#### `DocumentDetailContent.tsx`

```typescript
// 新增状态
const [archivePreview, setArchivePreview] = useState<{
  attId: string;
  fileName: string;
} | null>(null);

// 修改 handlePreview
const handlePreview = (attId: string, fileName: string) => {
  const ext = fileName.split('.').pop()?.toLowerCase() || '';
  
  // PDF — 保持现有逻辑
  if (ext === 'pdf') {
    const token = useAuthStore.getState().token;
    if (!token) { alert('请重新登录'); return; }
    window.open(
      `/api/v2/attachments/${attId}/preview?token=${encodeURIComponent(token)}`,
      '_blank'
    );
    return;
  }
  
  // 压缩包 — 弹出树形预览
  if (['zip', 'tar', 'gz', 'tgz'].includes(ext)) {
    setArchivePreview({ attId, fileName });
    return;
  }
  
  alert('该格式暂不支持预览');
};

// 渲染 Modal
return (
  <>
    {/* ... 现有内容 ... */}
    
    {/* 压缩包预览弹窗 */}
    {archivePreview && (
      <ArchiveTreeModal
        open={!!archivePreview}
        onClose={() => setArchivePreview(null)}
        attachmentId={archivePreview.attId}
        fileName={archivePreview.fileName}
        token={useAuthStore.getState().token!}
      />
    )}
  </>
);
```

#### `EntityDocumentSection.tsx`

同样的修改模式——扩展 `handlePreviewAttachment` 函数。

### 7.5 `ArchiveTreeModal.tsx` — 树形预览弹窗

```tsx
import { useState, useEffect } from 'react';
import { Modal } from './Modal';
import { attachmentsApi } from '../services/api';
import type { ArchiveTreeNode, ArchiveTreeResponse } from '../types';

interface ArchiveTreeModalProps {
  open: boolean;
  onClose: () => void;
  attachmentId: string;
  fileName: string;
  token: string;
}

/** 文件大小格式化 */
function formatSize(bytes: number): string {
  if (bytes < 1024) return `${bytes} B`;
  if (bytes < 1048576) return `${(bytes / 1024).toFixed(1)} KB`;
  return `${(bytes / 1048576).toFixed(1)} MB`;
}

/** 递归渲染树节点 */
function TreeNode({ node, depth = 0 }: { node: ArchiveTreeNode; depth?: number }) {
  const [expanded, setExpanded] = useState(depth < 1); // 第一层默认展开
  const isDir = node.type === 'dir';
  const hasChildren = isDir && (node.children?.length ?? 0) > 0;

  return (
    <>
      <tr
        className={`hover:bg-gray-50 ${isDir ? 'cursor-pointer' : ''}`}
        onClick={() => isDir && setExpanded(!expanded)}
      >
        <td className="px-3 py-1.5 text-xs whitespace-nowrap" style={{ paddingLeft: `${16 + depth * 20}px` }}>
          <span className="inline-flex items-center gap-1">
            {hasChildren && (
              <span className="text-gray-400 w-3">{expanded ? '▼' : '▶'}</span>
            )}
            {!hasChildren && isDir && <span className="w-3" />}
            <span>{isDir ? '📁' : '📄'}</span>
            <span className={isDir ? 'font-medium' : ''}>{node.name}</span>
          </span>
        </td>
        <td className="px-3 py-1.5 text-xs text-gray-500 w-24 text-right">
          {isDir ? '-' : formatSize(node.size)}
        </td>
      </tr>
      {expanded && node.children?.map((child, i) => (
        <TreeNode key={`${child.name}-${i}`} node={child} depth={depth + 1} />
      ))}
    </>
  );
}

export default function ArchiveTreeModal({
  open, onClose, attachmentId, fileName, token
}: ArchiveTreeModalProps) {
  const [data, setData] = useState<ArchiveTreeResponse | null>(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState('');

  useEffect(() => {
    if (!open || !attachmentId || !token) return;
    setLoading(true);
    setError('');
    attachmentsApi.archiveTree(attachmentId, token)
      .then(res => setData(res.data))
      .catch(() => setError('读取压缩包失败'))
      .finally(() => setLoading(false));
  }, [open, attachmentId, token]);

  return (
    <Modal open={open} title={`压缩包预览：${fileName}`} onClose={onClose} width="lg">
      {loading ? (
        <div className="py-8 text-center text-sm text-gray-400">读取中...</div>
      ) : error ? (
        <div className="py-8 text-center text-sm text-red-500">{error}</div>
      ) : data ? (
        <>
          <div className="flex gap-4 mb-3 px-2 text-xs text-gray-500">
            <span>共 {data.total_files} 个文件</span>
            <span>总大小 {formatSize(data.total_size)}</span>
          </div>
          <div className="border rounded-lg overflow-auto max-h-[60vh]">
            <table className="w-full text-sm">
              <thead>
                <tr className="bg-gray-50 text-xs text-gray-500 border-b">
                  <th className="px-3 py-1.5 text-left">名称</th>
                  <th className="px-3 py-1.5 text-right w-24">大小</th>
                </tr>
              </thead>
              <tbody className="divide-y divide-gray-50">
                {data.tree.map((node, i) => (
                  <TreeNode key={i} node={node} depth={0} />
                ))}
              </tbody>
            </table>
          </div>
        </>
      ) : null}
    </Modal>
  );
}
```

## 八、边缘情况处理

| 场景 | 处理方式 |
|------|----------|
| 空压缩包（0 个文件） | 正常返回空树，前端显示"共 0 个文件" |
| 超大压缩包（10000+ 文件） | 全量返回 JSON，前端树形折叠避免渲染瓶颈 |
| ZIP 密码保护 | `zipfile` 不提供密码则抛异常 → 500 错误 |
| 损坏的压缩包 | `BadZipFile` / `ReadError` → 500 返回友好错误信息 |
| 非压缩包扩展名（如 `.exe` 误标记为 `.zip`） | 后端格式检查失败 → 400 |
| 文件名含中文/特殊字符 | `zipfile` 默认 UTF-8，`tarfile` 默认 UTF-8，正常处理 |
| 嵌套多层目录 | 递归 `_build_tree` 支持任意深度 |
| Token 过期 | 返回 401（与现有 preview 一致） |

## 九、开发清单

| # | 文件 | 操作 | 预估行数 |
|---|------|------|----------|
| 1 | `backend/app/bom/archive_reader.py` | 新建 | ~100 行 |
| 2 | `backend/app/routers/attachments_v2.py` | 新增端点 + import | ~50 行 |
| 3 | `frontend/src/types/index.ts` | 新增类型 | ~15 行 |
| 4 | `frontend/src/services/api.ts` | 新增 API 方法 | ~5 行 |
| 5 | `frontend/src/components/ArchiveTreeModal.tsx` | 新建 | ~90 行 |
| 6 | `frontend/src/components/DocumentDetailContent.tsx` | 修改预览入口 | ~15 行 |
| 7 | `frontend/src/components/EntityDocumentSection.tsx` | 修改预览入口 | ~15 行 |
| **合计** | | | **~290 行** |

## 十、后续扩展

- **RAR 支持**: `pip install rarfile` → 新增 `_read_rar()` 方法
- **7Z 支持**: `pip install py7zr` → 新增 `_read_7z()` 方法
- **文件提取**: 在树中点击单个文件 → 后端解压该文件到临时目录 → 返回下载链接
- **全文搜索**: 在压缩包内按文件名搜索

---

*文档版本: v1.0 — 2026-05-09*
