# myPDM 系统用户权限说明

> **事实源**：权限定义的唯一权威来源为 `permissions/permissions.json`，本文档为其人类可读镜像。
> **代码生成**：`python tools/gen_permissions.py` → 后端 `backend/app/permissions/_generated.py` / 前端 `frontend/src/constants/permissions.generated.ts`（`npm run build` 的 prebuild 钩子自动执行）。
> **版本**：v2.1 —— 在 v2.0 审计基础上完成 18 项问题修复，详见第六章。
> **对应代码**：v3.1.3，「Master → Revision → Iteration」三层数据模型。
> **最后更新**：2026-07-30

---

## 一、权限体系总览

### 1.1 两层判定：角色门 + 对象策略

```
请求 → OAuth2 Bearer → get_current_user（解 JWT、查用户）
     → get_current_active_user（status == "active"）
     → require_permission("perm:action")        ← 第一层：角色门（查 PERMISSIONS 矩阵）
     → enforce_object_policy("policy_name", …)  ← 第二层：对象策略（路由内手工调用）
```

- **第一层（角色门）**：`require_permission(perm)` 依赖注入，只看 `user.role ∈ PERMISSIONS[perm]`。启动时对未定义权限名 fail-fast（`KeyError`）。
- **第二层（对象策略）**：判定"这个具体对象你能不能动"（创建者 / 审批人 / 保管人 / 单据参与者 / 项目经理 / 文件夹所有者 / 用户组成员）。

> ⚠️ **重要机制约束**：`permissions.json` 中的 `object_policy` 字段只会生成一份**声明式清单** `OBJECT_POLICIES`（`_generated.py`），`require_permission()` **不会自动执行它**（见 `backend/app/permissions/__init__.py:22-32`）。策略是否真正生效，取决于路由函数里有没有手工调用 `enforce_object_policy(...)`。
>
> 为防止"声明了却没人调用"长期潜伏，已加静态防回归测试
> `backend/tests/test_permission_wiring.py`：每条声明的策略都必须能在
> `backend/app/` 下找到对应的 `enforce/check_object_policy` 调用，否则测试失败。

### 1.2 角色定义

| 角色 | 标识 | 定位 |
|---|---|---|
| 管理员 | `admin` | 全部权限；用户/用户组管理、系统设置、自定义字段定义、操作日志、软删除清理、强制签入 |
| 工程师 | `engineer` | 核心业务读写：零部件/图文档/构型/BOM/变更/库存/项目；可删除关联、附件与 BOM 行，不可删除主对象 |
| 生产人员 | `production` | 查询、下载、导出；可参与库存单据全流程与项目任务；可查看零部件附件 |
| 访客 | `guest` | 只读；不可导出、不可评论、不可进入 BOM 工具、3D 预览、压缩包浏览、用户管理 |

**独立于系统角色的第二套角色**：项目模块内另有「**经理 / 成员**」（`project_members.role_in_project`），两者叠加生效，详见 3.9。

### 1.3 三层数据模型对权限的影响

零部件、图文档、构型项均为 `Master → Revision → Iteration` 三层结构，这直接决定了权限的作用位置：

| 层 | 权限相关字段 | 说明 |
|---|---|---|
| Master | — | 稳定身份（编号/名称）。删除 Master 需 `*:delete`（仅 admin）。图文档的**用户组关联挂在此层** |
| **Revision** | `check_out_user_id`、`status` | **权限判定的主战场**：几乎所有端点以 `revision_id` 为路径参数；签出锁在此层 |
| Iteration | `creator_id` | 「谁做了这次修改」记在迭代上（v3.1.3 起从 Master/Revision 下移） |

**签出锁 = 一条隐式对象策略**：签入 / 撤销签出 / 编辑内容一律要求 `check_out_user_id == 当前用户`，由各模块 CRUD 内联判定（`crud_parts.py:468`、`:500`；`routers/documents.py:266`；`routers/configuration.py:362` 等）。仅 `*:force_checkin`（admin）可越过。

**`creator_id` 的正确读法**：凡涉及"创建者"的权限判定，都必须**回溯迭代层**，不能读
Master/Revision 上的同名属性（那里没有这一列，`getattr` 恒为 `None`，会静默失效）。
图文档侧的统一入口是 `crud_groups.get_document_creator_id()`。

---

## 二、权限矩阵（由 permissions.json 生成）

> ✅ = 该角色在角色门通过；❌ = 403。通过角色门后，若"对象策略"列非空，还需满足该策略。
> 共 163 项权限。「新增 / v2.1」标注为本轮修复引入的变化。

#### 零部件（parts）

| 权限 | admin | engineer | production | guest | 对象策略 | 备注 |
|---|:-:|:-:|:-:|:-:|---|---|
| `parts:create` | ✅ | ✅ | ❌ | ❌ | — |  |
| `parts:delete` | ✅ | ❌ | ❌ | ❌ | — |  |
| `parts:read` | ✅ | ✅ | ✅ | ✅ | — |  |
| `parts:update` | ✅ | ✅ | ❌ | ❌ | — |  |
| `parts.doc:link` | ✅ | ✅ | ❌ | ❌ | — |  |
| `parts.doc:read` | ✅ | ✅ | ✅ | ✅ | — |  |
| `parts.doc:unlink` | ✅ | ✅ | ❌ | ❌ | — |  |
| `parts:export` | ✅ | ✅ | ✅ | ❌ | — | **新增** |
| `parts:checkout` | ✅ | ✅ | ❌ | ❌ | — |  |
| `parts:checkin` | ✅ | ✅ | ❌ | ❌ | — |  |
| `parts:undocheckout` | ✅ | ✅ | ❌ | ❌ | — |  |
| `parts:force_checkin` | ✅ | ❌ | ❌ | ❌ | — |  |
| `parts:upgrade` | ✅ | ✅ | ❌ | ❌ | — |  |
| `parts:release` | ✅ | ✅ | ❌ | ❌ | — |  |
| `parts:freeze` | ✅ | ✅ | ❌ | ❌ | — |  |
| `parts:unfreeze` | ✅ | ❌ | ❌ | ❌ | — |  |
| `parts:obsolete` | ✅ | ✅ | ❌ | ❌ | — |  |
| `parts:cascade_checkout` | ✅ | ✅ | ❌ | ❌ | — |  |
| `parts:cascade_checkin` | ✅ | ✅ | ❌ | ❌ | — |  |
| `parts:cascade_undocheckout` | ✅ | ✅ | ❌ | ❌ | — |  |

#### 图文档（documents）

| 权限 | admin | engineer | production | guest | 对象策略 | 备注 |
|---|:-:|:-:|:-:|:-:|---|---|
| `documents:read` | ✅ | ✅ | ✅ | ✅ | — |  |
| `documents:read_refs` | ✅ | ✅ | ✅ | ✅ | — |  |
| `documents:create` | ✅ | ✅ | ❌ | ❌ | — |  |
| `documents:update` | ✅ | ✅ | ❌ | ❌ | — |  |
| `documents:delete` | ✅ | ❌ | ❌ | ❌ | — |  |
| `documents.attachment:upload` | ✅ | ✅ | ❌ | ❌ | — |  |
| `documents.attachment:download` | ✅ | ✅ | ✅ | ✅ | — |  |
| `documents.attachment:delete` | ✅ | ✅ | ❌ | ❌ | — |  |
| `documents:checkout` | ✅ | ✅ | ❌ | ❌ | — |  |
| `documents:checkin` | ✅ | ✅ | ❌ | ❌ | — |  |
| `documents:undocheckout` | ✅ | ✅ | ❌ | ❌ | — |  |
| `documents:force_checkin` | ✅ | ❌ | ❌ | ❌ | — |  |

#### 附件 V2（attachments）

| 权限 | admin | engineer | production | guest | 对象策略 | 备注 |
|---|:-:|:-:|:-:|:-:|---|---|
| `attachments:list` | ✅ | ✅ | ✅ | ✅ | — | v2.1 放开到 production/guest（原 admin+engineer） |
| `attachments:upload` | ✅ | ✅ | ❌ | ❌ | — |  |
| `attachments:download` | ✅ | ✅ | ✅ | ✅ | — |  |
| `attachments:preview` | ✅ | ✅ | ✅ | ✅ | — | 仅用于签发媒体令牌 |
| `attachments:direct_download` | ✅ | ✅ | ✅ | ✅ | — | 仅用于签发媒体令牌 |
| `attachments:gltf` | ✅ | ✅ | ✅ | ❌ | — | 仅用于签发媒体令牌 |
| `attachments:archive_browse` | ✅ | ✅ | ✅ | ❌ | — | 仅用于签发媒体令牌 |
| `attachments:delete` | ✅ | ✅ | ❌ | ❌ | — |  |
| `attachments:convert_manage` | ✅ | ❌ | ❌ | ❌ | — |  |

#### BOM（bom）

| 权限 | admin | engineer | production | guest | 对象策略 | 备注 |
|---|:-:|:-:|:-:|:-:|---|---|
| `bom:tree` | ✅ | ✅ | ✅ | ❌ | — |  |
| `bom:compare` | ✅ | ✅ | ✅ | ❌ | — |  |
| `bom:trace` | ✅ | ✅ | ✅ | ❌ | — |  |
| `bom:doc_refs` | ✅ | ✅ | ✅ | ❌ | — |  |
| `bom:export` | ✅ | ✅ | ✅ | ❌ | — |  |
| `bom:create_relation` | ✅ | ✅ | ❌ | ❌ | — |  |
| `bom:delete_relation` | ✅ | ✅ | ❌ | ❌ | — | v2.1 放开到 engineer（原仅 admin） |

#### 构型项（configuration）

| 权限 | admin | engineer | production | guest | 对象策略 | 备注 |
|---|:-:|:-:|:-:|:-:|---|---|
| `configuration:read` | ✅ | ✅ | ✅ | ✅ | — |  |
| `configuration:create` | ✅ | ✅ | ❌ | ❌ | — |  |
| `configuration:update` | ✅ | ✅ | ❌ | ❌ | — |  |
| `configuration:delete` | ✅ | ❌ | ❌ | ❌ | — |  |
| `configuration.doc:manage` | ✅ | ✅ | ❌ | ❌ | — |  |
| `configuration:checkout` | ✅ | ✅ | ❌ | ❌ | — |  |
| `configuration:checkin` | ✅ | ✅ | ❌ | ❌ | — |  |
| `configuration:undocheckout` | ✅ | ✅ | ❌ | ❌ | — |  |
| `configuration:force_checkin` | ✅ | ❌ | ❌ | ❌ | — |  |
| `configuration.item:manage` | ✅ | ✅ | ❌ | ❌ | — |  |

#### 配置方案（profile）

| 权限 | admin | engineer | production | guest | 对象策略 | 备注 |
|---|:-:|:-:|:-:|:-:|---|---|
| `profile:read` | ✅ | ✅ | ✅ | ✅ | — |  |
| `profile:create` | ✅ | ✅ | ❌ | ❌ | — |  |
| `profile:update` | ✅ | ✅ | ❌ | ❌ | — |  |
| `profile:delete` | ✅ | ❌ | ❌ | ❌ | — |  |
| `profile:activate_archive` | ✅ | ✅ | ❌ | ❌ | — |  |
| `profile:approve` | ✅ | ✅ | ✅ | ❌ | profile_approver_or_admin | **新增** |
| `profile:cc_manage` | ✅ | ✅ | ✅ | ✅ | — | **新增** |
| `profile:change_status` | ✅ | ❌ | ❌ | ❌ | — |  |
| `profile.bom:manage` | ✅ | ✅ | ❌ | ❌ | — |  |

#### 变更请求 ECR

| 权限 | admin | engineer | production | guest | 对象策略 | 备注 |
|---|:-:|:-:|:-:|:-:|---|---|
| `ecr:read` | ✅ | ✅ | ✅ | ✅ | — |  |
| `ecr:read_status_log` | ✅ | ✅ | ✅ | ✅ | — |  |
| `ecr:bom_trace` | ✅ | ✅ | ✅ | ✅ | — |  |
| `ecr:cc_manage` | ✅ | ✅ | ✅ | ✅ | — |  |
| `ecr:create` | ✅ | ✅ | ❌ | ❌ | — |  |
| `ecr:update` | ✅ | ✅ | ❌ | ❌ | ecr_owner_or_admin |  |
| `ecr:delete` | ✅ | ✅ | ❌ | ❌ | ecr_owner_or_admin |  |
| `ecr:submit` | ✅ | ✅ | ❌ | ❌ | ecr_owner_or_admin |  |
| `ecr:withdraw` | ✅ | ✅ | ❌ | ❌ | ecr_owner_or_admin |  |
| `ecr:approve` | ✅ | ✅ | ❌ | ❌ | ecr_approver_or_admin |  |
| `ecr:close` | ✅ | ✅ | ❌ | ❌ | — |  |

#### 变更指令 ECO

| 权限 | admin | engineer | production | guest | 对象策略 | 备注 |
|---|:-:|:-:|:-:|:-:|---|---|
| `eco:read` | ✅ | ✅ | ✅ | ✅ | — |  |
| `eco:read_status_log` | ✅ | ✅ | ✅ | ✅ | — |  |
| `eco:bom_trace` | ✅ | ✅ | ✅ | ✅ | — |  |
| `eco:cc_manage` | ✅ | ✅ | ✅ | ✅ | — |  |
| `eco:create` | ✅ | ✅ | ❌ | ❌ | — |  |
| `eco:update` | ✅ | ✅ | ❌ | ❌ | eco_owner_or_admin |  |
| `eco:delete` | ✅ | ✅ | ❌ | ❌ | eco_owner_or_admin |  |
| `eco:submit` | ✅ | ✅ | ❌ | ❌ | eco_owner_or_admin |  |
| `eco:withdraw` | ✅ | ✅ | ❌ | ❌ | eco_owner_or_admin |  |
| `eco:close` | ✅ | ✅ | ❌ | ❌ | — |  |
| `eco:execute` | ✅ | ✅ | ❌ | ❌ | — |  |
| `eco:execute_item` | ✅ | ✅ | ❌ | ❌ | — |  |
| `eco:execute_all` | ✅ | ✅ | ❌ | ❌ | — |  |
| `eco:revise` | ✅ | ✅ | ❌ | ❌ | — |  |
| `eco:restore` | ✅ | ✅ | ❌ | ❌ | — |  |
| `eco:freeze` | ✅ | ✅ | ❌ | ❌ | — |  |
| `eco:publish` | ✅ | ✅ | ❌ | ❌ | — |  |
| `eco.affected:manage` | ✅ | ✅ | ❌ | ❌ | — |  |

#### 库存（inventory）

| 权限 | admin | engineer | production | guest | 对象策略 | 备注 |
|---|:-:|:-:|:-:|:-:|---|---|
| `inventory.warehouse:read` | ✅ | ✅ | ✅ | ✅ | — |  |
| `inventory.warehouse:write` | ✅ | ✅ | ❌ | ❌ | — |  |
| `inventory.warehouse:delete` | ✅ | ❌ | ❌ | ❌ | — |  |
| `inventory.material:read` | ✅ | ✅ | ✅ | ✅ | — |  |
| `inventory.material:write` | ✅ | ✅ | ❌ | ❌ | — |  |
| `inventory.material:enable_from_pdm` | ✅ | ✅ | ❌ | ❌ | — |  |
| `inventory.material:delete` | ✅ | ❌ | ❌ | ❌ | — |  |
| `inventory.stock:read` | ✅ | ✅ | ✅ | ✅ | — |  |
| `inventory.doc:read` | ✅ | ✅ | ✅ | ✅ | — |  |
| `inventory.doc:write` | ✅ | ✅ | ✅ | ❌ | inventory_doc_participant_or_admin | v2.1 新增参与者策略 |
| `inventory.doc:delete` | ✅ | ✅ | ✅ | ❌ | inventory_doc_participant_or_admin | v2.1 新增参与者策略 |
| `inventory.doc:submit_withdraw_approve` | ✅ | ✅ | ✅ | ❌ | inventory_doc_participant_or_admin | v2.1 新增参与者策略 |
| `inventory.doc:post` | ✅ | ✅ | ✅ | ❌ | inventory_keeper_or_admin |  |

#### 项目管理（project）

| 权限 | admin | engineer | production | guest | 对象策略 | 备注 |
|---|:-:|:-:|:-:|:-:|---|---|
| `project:read` | ✅ | ✅ | ✅ | ✅ | — |  |
| `project:create` | ✅ | ✅ | ❌ | ❌ | — |  |
| `project:update` | ✅ | ✅ | ❌ | ❌ | project_manager_or_admin |  |
| `project:delete` | ✅ | ✅ | ❌ | ❌ | project_manager_or_admin |  |
| `project.member:manage` | ✅ | ✅ | ❌ | ❌ | project_manager_or_admin |  |
| `project.task:create` | ✅ | ✅ | ✅ | ❌ | project_manager_or_admin |  |
| `project.task:update` | ✅ | ✅ | ✅ | ❌ | project_manager_or_admin |  |
| `project.task:update_status` | ✅ | ✅ | ✅ | ❌ | — |  |
| `project.task:delete` | ✅ | ✅ | ✅ | ❌ | project_manager_or_admin |  |
| `project.task:link` | ✅ | ✅ | ✅ | ❌ | — |  |
| `project.task:depend` | ✅ | ✅ | ✅ | ❌ | project_manager_or_admin |  |
| `project.task:comment` | ✅ | ✅ | ✅ | ❌ | — | v2.1 去掉 guest（访客只读） |

#### 用户看板（dashboard）

| 权限 | admin | engineer | production | guest | 对象策略 | 备注 |
|---|:-:|:-:|:-:|:-:|---|---|
| `dashboard:read` | ✅ | ✅ | ✅ | ✅ | — |  |
| `dashboard.folder:create` | ✅ | ✅ | ✅ | ✅ | dashboard_folder_editor |  |
| `dashboard.folder:rename` | ✅ | ✅ | ✅ | ✅ | dashboard_folder_editor |  |
| `dashboard.folder:delete` | ✅ | ✅ | ✅ | ✅ | dashboard_folder_editor |  |
| `dashboard.folder:share` | ✅ | ✅ | ✅ | ✅ | dashboard_folder_editor |  |
| `dashboard.folder:unshare` | ✅ | ✅ | ✅ | ✅ | dashboard_folder_editor |  |
| `dashboard.item:add` | ✅ | ✅ | ✅ | ✅ | dashboard_folder_editor |  |
| `dashboard.item:delete` | ✅ | ✅ | ✅ | ✅ | dashboard_folder_editor |  |
| `dashboard:export_all` | ✅ | ❌ | ❌ | ❌ | — |  |
| `dashboard:import_all` | ✅ | ❌ | ❌ | ❌ | — |  |

#### AI 助手（assistant）

| 权限 | admin | engineer | production | guest | 对象策略 | 备注 |
|---|:-:|:-:|:-:|:-:|---|---|
| `assistant:chat` | ✅ | ✅ | ✅ | ✅ | — |  |
| `assistant:download_artifact` | ✅ | ✅ | ✅ | ✅ | — |  |

#### 用户（users）

| 权限 | admin | engineer | production | guest | 对象策略 | 备注 |
|---|:-:|:-:|:-:|:-:|---|---|
| `users:read` | ✅ | ✅ | ✅ | ✅ | — |  |
| `users:read_detail` | ✅ | ❌ | ❌ | ❌ | — |  |
| `users:create` | ✅ | ❌ | ❌ | ❌ | — |  |
| `users:update` | ✅ | ❌ | ❌ | ❌ | — |  |
| `users:delete` | ✅ | ❌ | ❌ | ❌ | — |  |

#### 用户组（user_groups）

| 权限 | admin | engineer | production | guest | 对象策略 | 备注 |
|---|:-:|:-:|:-:|:-:|---|---|
| `user_groups:read` | ✅ | ✅ | ✅ | ❌ | — |  |
| `user_groups:manage` | ✅ | ❌ | ❌ | ❌ | — |  |

#### 操作日志（logs）

| 权限 | admin | engineer | production | guest | 对象策略 | 备注 |
|---|:-:|:-:|:-:|:-:|---|---|
| `logs:read` | ✅ | ❌ | ❌ | ❌ | — |  |

#### 自定义字段（custom_field）

| 权限 | admin | engineer | production | guest | 对象策略 | 备注 |
|---|:-:|:-:|:-:|:-:|---|---|
| `custom_field.def:read` | ✅ | ✅ | ✅ | ✅ | — |  |
| `custom_field.def:write` | ✅ | ❌ | ❌ | ❌ | — |  |
| `custom_field.def:sort` | ✅ | ❌ | ❌ | ❌ | — |  |
| `custom_field.value:read` | ✅ | ✅ | ✅ | ✅ | — |  |
| `custom_field.value:write` | ✅ | ✅ | ❌ | ❌ | — |  |
| `custom_field:reset_data` | ✅ | ❌ | ❌ | ❌ | — |  |

#### 数据管理（admin）

| 权限 | admin | engineer | production | guest | 对象策略 | 备注 |
|---|:-:|:-:|:-:|:-:|---|---|
| `admin.soft_delete:read` | ✅ | ❌ | ❌ | ❌ | — |  |
| `admin.soft_delete:cleanup` | ✅ | ❌ | ❌ | ❌ | — |  |

#### 数据同步（sync）

| 权限 | admin | engineer | production | guest | 对象策略 | 备注 |
|---|:-:|:-:|:-:|:-:|---|---|
| `sync:read` | ✅ | ✅ | ✅ | ✅ | — |  |

#### 通知中心（notifications）

| 权限 | admin | engineer | production | guest | 对象策略 | 备注 |
|---|:-:|:-:|:-:|:-:|---|---|
| `notifications:read` | ✅ | ✅ | ✅ | ✅ | — |  |
| `notifications:manage_own` | ✅ | ✅ | ✅ | ✅ | — | **新增** |

#### 导航可见性（nav，前端专用）

| 权限 | admin | engineer | production | guest | 对象策略 | 备注 |
|---|:-:|:-:|:-:|:-:|---|---|
| `nav.dashboard` | ✅ | ✅ | ✅ | ✅ | — | **新增** |
| `nav.board` | ✅ | ✅ | ✅ | ✅ | — | **新增** |
| `nav.configuration` | ✅ | ✅ | ✅ | ✅ | — | **新增** |
| `nav.parts` | ✅ | ✅ | ✅ | ✅ | — | **新增** |
| `nav.documents` | ✅ | ✅ | ✅ | ✅ | — | **新增** |
| `nav.ec` | ✅ | ✅ | ✅ | ✅ | — | **新增** |
| `nav.inventory` | ✅ | ✅ | ✅ | ✅ | — | **新增** |
| `nav.projects` | ✅ | ✅ | ✅ | ✅ | — | **新增** |
| `nav.users` | ✅ | ✅ | ✅ | ❌ | — | **新增** |
| `nav.settings` | ✅ | ✅ | ✅ | ✅ | — | **新增** |
| `nav.help` | ✅ | ✅ | ✅ | ✅ | — | **新增** |

---

## 三、各模块权限行为（矩阵之外的规则）

### 3.1 零部件（`/api/parts`，`routers/parts.py`）

- 列表 / 详情 / 版本 / 迭代查询：`parts:read`（全角色）。
- 生命周期端点各有独立权限：`checkout` / `checkin` / `undocheckout` / `upgrade` / `release` / `freeze` / `obsolete` / `cascade_*`（admin+engineer），`force_checkin` / `unfreeze` 仅 admin。
- 状态前置条件由 CRUD 强制：仅 `draft` 可签出；被签出的版本不可发布；只有签出者本人可签入 / 撤销签出。
- BOM 端点复用 BOM 权限：读 `bom:tree`（排除 guest），增改 `bom:create_relation`，删 `bom:delete_relation`（admin+engineer）。
- 附件端点：列表与文件读取全角色（`attachments:list` / `attachments:download`），上传 `attachments:upload`、删除 `attachments:delete`（均 admin+engineer）。删除时校验「附件 → 迭代 → 版本」归属，并写操作日志。
- CAD 工作台：`POST /cad/bom-match` 用 `parts:read`，`POST /revisions/{id}/cad/bom-sync` 用 `parts:update`。

### 3.2 图文档（`/api/documents`）+ 用户组内容访问

除角色门外，图文档**附件内容**受用户组约束（`document_content_access` 策略，`crud_groups.py`）：

| 条件 | 结果 |
|---|---|
| 用户为 admin | 允许 |
| 文档创建者本人（回溯最早迭代的 `creator_id`） | 允许 |
| 文档未关联任何用户组 | 允许（留空 = 全员可访问） |
| 用户所属组 ∩ 文档关联组 ≠ ∅ | 允许（多组为「或」关系） |
| 其他 | 403 |

- 生效范围：附件列表 / 下载 / 预览 / 3D / 压缩包浏览（`documents.py`、`attachments_v2.py` 各处）。
- **不隐藏文档元数据**：列表接口对每行返回 `group_ids` 与 `accessible` 标记（由
  `crud_groups.annotate_documents_access()` 批量补齐：用户组 1 次查询 + 组关联 1 次查询，
  仅对"会被拒绝"的行才回溯创建者），前端据此置灰 + 🔒 + 提供「可查看」筛选。
- 关联关系挂在 **Master** 上（`document_group_links.document_id → document_masters.id`），对全部版本与迭代统一生效。
- 组关联的写入点：新建文档时 `group_ids`；修改需先签出且为签出者本人。
- 用户组本身：`user_groups:read`（admin/engineer/production）、`user_groups:manage`（仅 admin）。删除用户组会级联删除 `DocumentGroupLink`，**原先仅关联该组的文档退化为全员可访问**。

### 3.3 附件 V2（`/api/v2/attachments`）与媒体令牌

浏览器需直连的操作（预览 / 原生下载 / glTF / 压缩包）使用**短寿命媒体令牌**，不把会话 JWT 放进 URL：

1. 前端先调 `GET /{id}/media-token?action=<action>`（`get_current_active_user` + 内联 `has_permission(_ACTION_PERM[action])` + 用户组访问校验）；
2. 拿到令牌后拼到目标 URL 的 `?token=`；
3. 目标端点只做 `verify_media_token(token, attachment_id, action)`，不再校验角色。

令牌 TTL 300 秒，载荷绑定 `aid` + `act` + `typ=media`，跨附件 / 跨动作复用会 403。

`action → 权限`：`preview`/`office-pdf` → `attachments:preview`；`direct-download` → `attachments:direct_download`；`gltf` → `attachments:gltf`；`archive-tree`/`extract-file` → `attachments:archive_browse`。

### 3.4 BOM（`/api/bom`）

`bom:*` 全部**排除 guest**。`bom:create_relation` 与 `bom:delete_relation` 均为 admin+engineer（增删对称）。

### 3.5 构型项与配置方案（`/api/configurations`）

- 构型项：读全角色；增删改与 `configuration.item:manage` / `configuration.doc:manage` 为 admin+engineer；`delete` 与 `force_checkin` 仅 admin；有完整签入签出链。
- 配置方案流程：`submit` / `withdraw` / `archive` 用 `profile:activate_archive`；`status` 直改用 `profile:change_status`（仅 admin）；`reopen` 用 `profile:update`。
- **审批**：`POST /profiles/{id}/review` 用 `profile:approve`（admin/engineer/production，排除 guest）+ 对象策略 `profile_approver_or_admin`；`crud_configuration.review_profile` 内的审批人校验保留作纵深防御。
- **抄送**：`POST|DELETE /profiles/{id}/cc` 用 `profile:cc_manage`（全角色，与 `ecr:cc_manage` 对齐）。

### 3.6 变更管理 ECR / ECO

- 读类（列表、详情、状态日志、BOM 追溯、抄送）全角色；写类 admin+engineer。
- 对象策略：ECR 的 update/delete/submit/withdraw → `ecr_owner_or_admin`，review → `ecr_approver_or_admin`；ECO 的 update/delete/submit/withdraw → `eco_owner_or_admin`。
- ECO 的 `review` 端点挂 `eco:update` + owner 策略（不是"审批人"策略）。
- PDF 导出由**前端生成**（`services/ecPdfExport.ts`），后端无对应端点。

### 3.7 库存（`/api/inventory`）

- 仓库 / 物料：读全角色，写 admin+engineer，删仅 admin。
- 单据：读全角色，`write` / `delete` / `submit_withdraw_approve` 含 production。
- **单据级门禁 `inventory_doc_participant_or_admin`**：admin / 创建者 / 保管人 / 指定审批人。
  由 `routers/inventory.py` 的 `_enforce_doc_participant()` 在 8 个单据端点统一调用
  （`GET/PUT/DELETE /documents/{id}`、`submit`、`withdraw`、`review`、`assign-keeper`、`cancel`），
  与 `crud_inventory.list_documents` 的行级可见性**同口径** —— 列表里看不到的单据，
  拿到 UUID 也读不动。
- 过账 `post` 用更严的 `inventory_keeper_or_admin`（仅保管人或 admin）。
- 状态守卫：仅 `draft` 可编辑，仅 `draft`/`rejected` 可删除，仅 `reviewing` 可审批。

### 3.8 用户看板（`/api/dashboard`）

- 全部看板权限对四种角色开放，靠所有权 / 共享权限约束。
- **判定唯一入口**：`_check_folder_edit_permission(folder, user, db)` —— 算出「看板所有者」+
  「本文件夹 ∪ 全部祖先上被授予 edit 的用户集合」，再交给 `dashboard_folder_editor` 策略。
  文件夹改名 / 删除 / item 增删 / 共享管理全部走这一个入口，口径一致。
- 策略只接受显式关键字参数（`owner_user_id=` / `editor_user_ids=`），不从对象属性推断
  （`DashboardFolder` 既无 owner 列，也无法表达祖先继承）。
- 共享管理类端点另有更严的「仅看板所有者」前置检查。
- 根文件夹删除额外要求 admin；看板全量导入导出仅 admin。

### 3.9 项目管理（`/api/projects`）—— 双层角色

| 项目角色 | 来源 | 能做什么 |
|---|---|---|
| 经理 | `role_in_project == "经理"`，或项目 owner，或系统 admin | 编辑/删除项目、成员管理、任务增删改与移动排序、依赖与自动排期、更新任意任务状态、删除任何评论 |
| 成员 | `role_in_project == "成员"` | 查看项目/任务/甘特/交付物、任务关联对象增删、发表评论（仅删本人）、**仅更新自己负责的任务状态** |

- 管理者集合 = `{owner} ∪ {经理成员}`（`projects.py:41-47`），交给 `project_manager_or_admin` 策略的 `manager_ids` 参数。
- 非管理者但为成员的操作走 `_require_member`；非成员且非 admin 一律 403「非项目成员」。
- 项目可见性：`list_projects` 对非 admin 只返回自己参与的项目。
- owner 恒为经理，角色不可下调、不可移除；移除成员会清空其名下任务负责人。
- 靠路由内联判定的三项：`project.task:update_status`（经理或任务负责人）、`project.task:link`、`project.task:comment`（成员即可；删评论时非经理仅限本人）。
- **前端已按项目角色渲染**：`GET /projects/{id}` 返回 `is_manager` 与 `my_role_in_project`，
  `Projects.tsx` 的 `isManager = can('project.task:create') && currentProject.is_manager`，
  「成员」不再看到管理类按钮。
- 系统角色与项目角色**叠加生效**：`production` 即使是项目经理也无 `project:update` /
  `project.member:manage`（这两项只给 admin+engineer）；`guest` 可读项目但不可评论。

### 3.10 通知中心（`/api/notifications`）

读用 `notifications:read`，写（标记已读 / 全部已读 / 清除已读）用 `notifications:manage_own`，两者均全角色开放；所有操作以 `current_user.id` 为作用域，只能读写自己的通知。

### 3.11 AI 助手（`/api/assistant`）

`assistant:chat` / `assistant:download_artifact` 全角色开放。**不构成权限绕过**：助手调用业务接口时通过进程内 ASGI 转发并**携带调用者本人的 token**（`assistant/api_gateway.py:74-96`），权限在真实端点重判；另有只读白名单 `is_allowed(path)` 与按角色过滤的接口目录。

### 3.12 用户 / 用户组 / 日志 / 自定义字段 / 数据管理 / 同步

- `users:read`（列表）对全角色开放 —— 看板共享选人、任务指派、审批人/保管人选择都依赖它；
  但**手机号仅 admin 与本人可见**（`routers/users.py` 对非 admin 置 `phone = None`）。
  `users:read_detail` 及所有写操作仅 admin。管理员通过 `PUT /users/{id}` 传 `password`
  即为重置密码，并置 `must_change_password = true`。
- 用户导入导出为纯前端实现（数据源为 `users:read`），UI 上以 `isAdmin()` 限制。
- 自定义字段：定义的读全角色，定义的写 / 排序 / 重置仅 admin；字段值的写为 admin+engineer。
- 操作日志 `logs:read`、软删除统计与清理 `admin.soft_delete:*`：仅 admin。
- 数据同步只有 `GET /api/sync/status`（`sync:read`，全角色）。
- 系统配置 `GET /api/settings/cad-naming` 挂 `parts:read`（需登录）。

---

## 四、对象级策略清单

### 4.1 已注册策略（`backend/app/permissions/policies.py`）

| 策略名 | 规则 | 声明于 permissions.json | 调用点 |
|---|---|---|---|
| `ecr_owner_or_admin` | admin 或 `ecr.creator_id == user.id` | ecr:update/delete/submit/withdraw | `ecrs.py` ×4 |
| `ecr_approver_or_admin` | admin 或 user ∈ `reviewer_ids` | ecr:approve | `ecrs.py` |
| `eco_owner_or_admin` | admin 或 `eco.creator_id == user.id` | eco:update/delete/submit/withdraw | `ecos.py` ×4 |
| `inventory_keeper_or_admin` | admin 或 `doc.keeper_id == user.id` | inventory.doc:post | `inventory.py` |
| `inventory_doc_participant_or_admin` | admin / 创建者 / 保管人 / 指定审批人 | inventory.doc:write/delete/submit_withdraw_approve | `inventory.py` `_enforce_doc_participant` ×8 |
| `profile_approver_or_admin` | admin 或 `profile.reviewers[].user_id` 命中 | profile:approve | `configuration.py` |
| `project_manager_or_admin` | admin 或 user ∈ `manager_ids`（owner + 经理成员） | project:update/delete、project.member:manage、project.task:create/update/delete/depend | `projects.py` `_enforce_manager` |
| `dashboard_folder_editor` | admin / 看板所有者 / 本文件夹或祖先上 edit 共享 | dashboard.folder:create/rename/delete/share/unshare、dashboard.item:add/delete | `dashboard.py` `_check_folder_edit_permission`（唯一入口） |
| `document_content_access` | admin / 创建者 / 未关联组 / 组交集非空 | 纯代码策略（不绑定单一权限项） | `crud_groups.py` → documents、attachments_v2 各处 |

> 所有策略的入参一律**显式传递**（`manager_ids=` / `reviewer_ids=` / `creator_id=` /
> `owner_user_id=` / `editor_user_ids=`），不从 ORM 对象属性推断 —— 属性推断在模型字段
> 搬迁后会静默失效，这正是 v2.0 审计发现的两类问题（#5、#12）。

### 4.2 未登记的隐式对象规则（代码里真实存在）

| 规则 | 位置 |
|---|---|
| 签出锁：仅签出者本人可签入 / 撤销签出 / 编辑内容 | `crud_parts.py:468,500`；`routers/documents.py:266,401,519`；`routers/configuration.py:362,627,676…` |
| 库存单据行级可见性：非 admin 仅见 创建/保管/待我审批 | `crud_inventory.py:391-397` |
| 项目成员门禁：非成员且非 admin 403 | `projects.py:36-38` |
| 项目任务状态：经理或任务负责人 | `projects.py:270` |
| 任务评论删除：经理或评论作者 | `projects.py:384` |
| 看板共享管理：仅看板所有者（比策略更严） | `dashboard.py:512,548,608,679` |
| 根文件夹删除需 admin | `dashboard.py:401` |
| 状态守卫（仅草稿可改/可删、被签出不可发布等） | 各 `crud_*.py` |

---

## 五、前端权限控制

### 5.1 判定函数（`frontend/src/stores/auth.ts`）

```typescript
can('perm:action')  // 推荐：查 permissions.generated.ts 的 PERMISSIONS[perm]
hasRole(['admin'])  // 直接比对角色
canEdit()      → can('parts:create')   // admin + engineer
canDownload()  → can('parts:export')   // admin + engineer + production（guest 无导出）
canPreview()   → true                  // 恒 true
isAdmin()      → can('parts:delete')   // 仅 admin
```

> `canDownload()` 绑定的 `parts:export` 是真实存在的权限项。注意导出（Excel/PDF）目前
> 全部由前端生成，此判定为 **UI 约束**；后端只保证读权限，若需强制约束导出，需另加后端导出端点。

### 5.2 侧边栏导航（`components/Layout.tsx`）

导航项声明 `perm: Permission`，可见性由 `can(item.perm)` 决定，与后端同一事实源：

| 导航项 | 权限 | 可见角色 |
|---|---|---|
| 仪表盘 / 用户看板 / 构型管理 / 零部件管理 / 图文档管理 / 变更管理 / 库存管理 / 项目管理 / 系统设置 / 帮助文档 | `nav.dashboard` … `nav.help` | 全部四种角色 |
| 用户管理 | `nav.users` | admin、engineer、production |

系统设置页内的「数据管理」「操作日志」页签额外用 `isAdmin()` 挡；`pages/DataManagement.tsx` 整页 `isAdmin()` 门禁。

### 5.3 前端权限只是 UI 约束

导出 Excel / PDF、用户导入导出等功能在**前端生成**（`services/importExport.ts`、`ecPdfExport.ts`、`configProfilePdfExport.ts`），数据来源是常规读接口。隐藏按钮 ≠ 接口保护。

---

## 六、v2.0 审计问题修复记录（18 项，全部已修）

> 审计版本 v2.0（2026-07-30）发现 17 项问题，修复过程中另发现 1 项（#18）。
> 实施计划：`docs/superpowers/plans/2026-07-30-permission-audit-fixes.md`

### P0 —— 未认证 / 越权

| # | 问题 | 修复 |
|---|---|---|
| 1 | `POST /api/parts/revisions/{id}/attachments` 无任何认证依赖，未登录者可上传任意文件 | 挂 `require_permission("attachments:upload")` |
| 2 | `DELETE /api/parts/revisions/{id}/attachments/{aid}` 无认证，未登录者凭 UUID 即可删库+删文件+删 GLB 缓存，且不校验附件归属 | 挂 `require_permission("attachments:delete")`；新增 `_attachment_belongs_to_revision()` 校验（附件→迭代→版本），不匹配 404；补操作日志 |
| 3 | `GET /api/settings/cad-naming` 完全未认证 | 挂 `require_permission("parts:read")` |

### P1 —— 功能性缺陷

| # | 问题 | 修复 |
|---|---|---|
| 4 | `attachments:list` 仅 admin+engineer，production/guest 打开零部件详情即 403，看不到图纸与生产附件 | 放开到全 4 角色；`.../attachments/{aid}/file` 改挂 `attachments:download`（语义归位）；写类仍 admin+engineer |
| 5 | `document_content_access` 的「创建者始终可访问」失效（`creator_id` 已迁到 Iteration，`DocumentMaster` 无此列，`getattr` 恒 None） | 策略改为显式接收 `creator_id`；新增 `crud_groups.get_document_creator_id()` 回溯迭代层；创建者查询做惰性化（仅在将被拒绝时才查） |
| 6 | `bom:delete_relation` 仅 admin，engineer 加得了 BOM 行却删不掉 | 放开到 engineer，与 `bom:create_relation` 对称 |
| 7 | 库存单据 `get_document` 无行级过滤，凭 UUID 可读写他人草稿单据 | 新增 `inventory_doc_participant_or_admin` 策略 + `_enforce_doc_participant()`，在 8 个单据端点调用，与列表过滤同口径 |

### P2 —— 设计不一致

| # | 问题 | 修复 |
|---|---|---|
| 8 | 配置方案审批端点挂只读权限 `profile:read`（guest 也过角色门） | 新增 `profile:approve`（admin/engineer/production）+ `profile_approver_or_admin` 策略 |
| 9 | 抄送写操作挂 `profile:read` | 新增 `profile:cc_manage`（全角色，对齐 `ecr:cc_manage`） |
| 10 | 通知 3 个写端点复用 `notifications:read` | 新增 `notifications:manage_own`（全角色），读写分离 |
| 11 | `OBJECT_POLICIES` 只是声明；看板 7 项声明只有 4 处调用，另 4 个端点走口径不同的第二套判定（祖先继承 vs 直接共享） | 两套合并为 `_check_folder_edit_permission` 单一入口；新增静态防回归测试 `test_permission_wiring.py`，声明未调用即测试失败 |
| 12 | 看板策略依赖给 ORM 对象临时挂 `owner_user_id`（模型无此列），漏挂即静默退化为"仅 admin" | 策略改为只接受显式关键字参数；新增测试断言"挂了同名属性也不生效" |
| 13 | 前端 `isManager` 实为 `can('project.task:create')`，不看项目角色，「成员」看得见管理按钮再吃 403 | 后端 `GET /projects/{id}` 返回 `is_manager` / `my_role_in_project`；前端双层判定 |

### P3 —— 冗余与脱钩

| # | 问题 | 修复 |
|---|---|---|
| 14 | 10 个死权限（后端无门禁、前端 `can()` 也不用） | 全部删除：`parts.bom:manage`、`parts.bom:export_single`、`parts.bom:import_export_all`、`documents:import_export_all`、`documents.attachment:preview`、`configuration:export`、`ecr:export_pdf`、`eco:export_pdf`、`users:reset_password`、`users:import_export`。新增 `test_no_dead_permissions` 防止再长出来 |
| 15 | 导航可见性硬编码 roles 数组，`nav.*` 权限定义了却没人用 | 删除 `nav.admin_tools`（菜单早已不存在）；新增 11 项与菜单一一对应的 `nav.*`；`Layout.tsx` 改用 `can(item.perm)` |
| 16 | `canDownload()` 绑到 `parts:read`（恒真），且旧文档引用的 `parts:export` 根本不存在 | 新增 `parts:export`（admin/engineer/production），`canDownload()` 绑定它 |
| 17 | `GET /api/users/` 对全角色开放且返回全员手机号 | 保留全角色可读（选人下拉依赖），但对非 admin 将 `phone` 置空（本人除外） |
| 18 | **修复过程中新发现**：图文档列表接口不返回 `group_ids` / `accessible`（三层模型迁移时丢失），前端的 🔒 标记与「可查看」筛选完全失效 | 新增 `crud_groups.annotate_documents_access()` 批量补齐（2 次查询，无 N+1），列表端点调用 |

### 与原建议的偏差（有意为之）

| 项 | 原建议 | 实际做法 | 原因 |
|---|---|---|---|
| #8 | 与 ECR 对齐 = admin+engineer | 保留 production，只排除 guest | 库存审批已含 production，现网可能已有 production 审批人；收紧会让存量审批流卡死 |
| #6 | 可加「仅草稿且本人签出」前置条件 | 不加 | `bom:create_relation` 也没有；先保持增删对称，要加应两侧同时加 |
| #17 | 可收紧 `users:read` 到 admin+engineer | 不收紧，改为字段脱敏 | guest 也能共享自己的看板文件夹，需要选人列表；收紧会连带破坏该功能 |
| #15 | 导航排除 guest 的项目模块 | 反向处理：导航放开 guest，去掉 `project.task:comment` 的 guest | 仪表盘会调 `/projects/my-tasks`；收紧 API 会让 guest 仪表盘报错。改为"可读不可写"更符合访客定位 |

### 顺带修复（非权限问题，同源于三层模型迁移）

- `PUT /api/dashboard/folders/{fid}/shares/{sid}`：`folder` 变量从未定义 → 所有者调用时
  必然 `NameError` 500。已补 folder 查询。
- `crud_deliverables` 的 `creator_name` 恒为空串（`user_names` 入参从未使用）：
  已改为回溯迭代层解析创建者。
- `tools/backfill_document_creator.py` 引用已不存在的 `Document.creator_id`：
  已改为回填 `document_iterations.creator_id`。
- **24 个既有失败测试**（`test_deliverables` 14 / `test_document_content_access` 6 /
  `test_user_groups_api` 4）全部修复：测试辅助函数改造为三层模型，并把
  `make_document` / `make_doc_attachment` / `make_user_group` 提升为 conftest 公共 fixture，
  避免再出现"用 `SimpleNamespace` 造对象掩盖模型字段搬迁"的情况（#5 就是这样漏掉的）。

---

## 七、注意事项

1. **前端权限只是 UI 控制**：隐藏按钮 ≠ 接口保护，所有写操作必须有后端门禁。
2. **两层判定都要写**：新增带 `object_policy` 的权限时，必须在路由里手工调用
   `enforce_object_policy`。`test_permission_wiring.py` 会拦住漏调用。
3. **策略入参必须显式传递**：不要依赖 ORM 对象属性（模型字段搬迁会让 `getattr` 静默返回 None）。
4. **测试请用真实 ORM 对象 / conftest 工厂**：`SimpleNamespace` 会掩盖模型字段变更导致的策略失效。
5. **用户状态**：`status != "active"` 的用户即使角色正确也无法通过 `get_current_active_user`。
6. **强制改密**：管理员替他人改密码后 `must_change_password = true`，该用户下次登录被引导到 `ForcePasswordChange`。
7. **权限变更流程**：改 `permissions/permissions.json` → `python tools/gen_permissions.py`
   （或 `cd frontend; npm run build` 自动触发）→ 前后端常量同步更新。
   切勿手改 `_generated.py` / `permissions.generated.ts`（`test_generated_matches_permissions_json` 会拦）。
8. **图文档用户组**：仅约束附件内容，不隐藏元数据；删除用户组会让原文档退化为全员可访问。

---

## 附录 A：路由器权限总览（v3.1.3）

| 路由器 | 前缀 | 端点数 | 读 | 写 | 删 | 特殊说明 |
|---|---|:-:|---|---|---|---|
| `auth.py` | `/api/auth` | 4 | 登录用户 | — | — | `/token`、`/refresh` 公开；`/me`、`/change-password` 仅需登录 |
| `parts.py` | `/api/parts` | 53 | 全角色 | admin+engineer | admin | 含完整生命周期与 CAD 工作台；附件端点已全部有门禁 |
| `documents.py` | `/api/documents` | 27 | 全角色 | admin+engineer | admin | 附件内容叠加用户组策略；列表带 `accessible` 标记 |
| `attachments_v2.py` | `/api/v2/attachments` | 20 | 全角色 | admin+engineer | admin+engineer | 预览/3D/压缩包走媒体令牌；转换管理仅 admin |
| `bom.py` | `/api/bom` | 9 | admin+engineer+production | admin+engineer | admin+engineer | guest 完全排除 |
| `configuration.py` | `/api/configurations` | 48 | 全角色 | admin+engineer | admin | 审批 `profile:approve` + 审批人策略 |
| `ecrs.py` | `/api/ecrs` | 16 | 全角色 | admin+engineer | admin+engineer | owner / approver 策略完整 |
| `ecos.py` | `/api/ecos` | 26 | 全角色 | admin+engineer | admin+engineer | owner 策略完整 |
| `inventory.py` | `/api/inventory` | 22 | 全角色 | 仓库/物料 admin+engineer；单据含 production | 仓库/物料 admin；单据含 production | 单据 8 端点叠加参与者策略；过账需保管人 |
| `projects.py` | `/api/projects` | 30 | 项目成员 | 经理 | 经理 | 双层角色；详情返回 `is_manager` |
| `dashboard.py` | `/api/dashboard` | 16 | 全角色 | 全角色 | 全角色 | 单一判定入口 + 所有者/祖先共享 |
| `notifications.py` | `/api/notifications` | 5 | `notifications:read` | `notifications:manage_own` | `notifications:manage_own` | 全部 self-scoped |
| `users.py` | `/api/users` | 7 | 列表全角色（手机号脱敏）；详情 admin | admin | admin | — |
| `user_groups.py` | `/api/user-groups` | 6 | admin+engineer+production | admin | admin | 删除组会使文档退化为全员可访问 |
| `custom_fields.py` | `/api/custom-fields` | 9 | 全角色 | 定义 admin；值 admin+engineer | admin | — |
| `logs.py` | `/api/logs` | 1 | admin | — | — | — |
| `admin.py` | `/api/admin` | 2 | admin | admin | — | 软删除管理 |
| `assistant.py` | `/api/assistant` | 2 | 全角色 | 全角色 | — | 转发携带调用者 token |
| `sync.py` | `/api/sync` | 1 | 全角色 | — | — | 仅 `/status` |
| `settings.py` | `/api/settings` | 1 | `parts:read` | — | — | `/cad-naming`（已补认证） |

> **全部 250 个端点均有认证门禁**（`/api/auth/token`、`/api/auth/refresh` 为登录入口，
> `/`、`/health` 为健康检查）。

---

## 附录 B：防回归测试

`backend/tests/test_permission_wiring.py`（静态扫描，7 项）：

| 测试 | 拦住什么 |
|---|---|
| `test_every_declared_object_policy_is_registered` | 声明了未注册的策略名 |
| `test_every_declared_object_policy_is_actually_enforced` | 声明了 object_policy 但没人调用（角色门静默放行） |
| `test_registered_policies_are_all_declared_or_documented` | 注册了却既未声明也未被调用的残留策略 |
| `test_no_dead_permissions` | 定义了但无任何引用的权限项 |
| `test_no_undefined_permission_referenced` | 路由引用了未定义的权限名 |
| `test_generated_matches_permissions_json` | 手改生成物 / 忘跑生成器 |
| `test_frontend_only_perms_have_no_backend_gate` | `nav.*` 被误挂到后端端点 |

`backend/tests/test_object_policies.py`：9 条策略的单元测试，含两条"不得从对象属性推断"的护栏。
`backend/tests/test_document_content_access.py`、`test_user_groups_api.py`：图文档用户组访问的
端到端与单元测试，含"创建者不在组内仍可访问"的回归护栏。
