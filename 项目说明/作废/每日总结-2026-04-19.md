# 每日总结 - 2026-04-19

## 今日概况

| 项目 | 内容 |
|------|------|
| 日期 | 2026-04-19 |
| 主要工作 | GitHub 首次提交 + BOM工具前端功能梳理 + bug 定位 |
| 投入时间 | 全天（断断续续） |

---

## 一、GitHub 首次提交与推送

### 完成内容

**建立 Git 仓库**，项目 `D:\OpenCode\myPDM` 初始化 Git 并推送到 GitHub：

```
仓库：https://github.com/guojunjiang-collab/myPDM
分支：main
Commit：2522b7a（首次提交）
时间：2026-04-19 22:05:06
作者：guojun <guojun.jiang@qq.com>
```

**首次提交包含 29 个文件，6711 行代码**：
- `.gitignore`（过滤 __pycache__、*.pyc、node_modules 等）
- `backend/` — FastAPI 后端（models、routers、schemas、crud）
- `frontend/index.html` — 单文件 SPA 前端
- `initdb/init.sql` — 数据库初始化
- `nginx/nginx.conf` — Nginx 配置
- `项目说明/` — 开发日志记录

### 注意事项

- `docker-compose.yml` 不在仓库中（已被 .gitignore 忽略），需从 MEMORY.md 启动命令启动
- 之后开发流程：`git add` → `git commit -m "描述"` → `git push`

---

## 二、前端功能现状梳理

### 文件信息

| 版本 | 文件大小 | 行数 | 说明 |
|------|----------|------|------|
| Git HEAD（今天提交版） | 201,393 bytes | 3,737 行 | 含今天改的正确内容 |
| Working Copy（昨晚覆盖版） | 205,130 bytes | 3,737 行 | 被昨晚旧版覆盖，内容更旧，多了 3,737 bytes 的 `\r` 字符 |

### 功能清单

#### ✅ 已实现的功能（在 Git HEAD 中）

| 功能 | 关键代码 | 说明 |
|------|----------|------|
| 检出数据按钮 | "检出数据" | 原"同步数据"改名 |
| BOM对比导出 | `_exportBom` | 递归展平BOM树导出CSV |
| BOM反查（searchUp树形） | `doReverse(rid, rtype)` | 支持零件+部件两种类型 |
| 零件+部件联合搜索 | `_filterPartList` | 同时搜索 parts 和 components |
| 附件字段 | `sourceFile` | 29处引用，含 sourceFile_data |
| 材料字段显示 | `dict_materials` | 9处，含 _fieldMap 映射 |
| 状态标签 | `statusTag` | 14处，含 CSS 样式 |
| 修订记录 | `revisions` | 24处，含编辑时字段变更记录 |
| BOM子项删除同步修复 | `removeAssemblyPart` | sp.id 修复，空数组 else 分支 |
| 同步面板 | `SyncPanel` | retryAllFailed / clearCompleted / togglePause |
| bom_items 全量同步 | `getAllBomItems` | 2处，API接口调用 |
| 字段映射 | `_fieldMap` | 7个实体的双向映射 |
| ID映射 | `_updateLocalId` | 1处，局部引用 |
| 编辑部件 | `_editComp` | 21处 |
| 查看部件 | `_viewComp` | 4处 |
| loss_rate 已移除 | loss_rate 0次 | ✅ 确认已清除 |

#### ⚠️ 已知问题（昨晚覆盖导致的差异）

| 问题 | 说明 |
|------|------|
| onclick 行导航双引号 Bug | 代码中有 `onclick="Pages._viewPart(\'' + p.id + '\');"` 但未生效（双引号嵌套问题）|
| table onclick 行导航 | `_viewComp` 的 onclick 行导航功能未验证 |
| _viewComp 点击行 → 详情 | 需在浏览器 F12 Console 验证 |

#### ❌ 计划中但未完成的功能（Git HEAD 本身也缺失）

| 功能 | 状态 | 说明 |
|------|------|------|
| `uploadAttachment` 独立API上传 | 未实现 | BOM子项→附件独立存储迁移未完成 |
| `_downloadAtt` 全局函数 | 未实现 | 登录页 ReferenceError 修复 |
| `_editComp done()` currentPart 修复 | 未实现 | 附件删除闭包 bug 修复 |
| `assembly_version` 版本管理 | 未实现 | 字母序列 A→B→C 版本自动递增 |
| 前端 sourceFile→source_file 替换 | 未实现 | 前端camelCase与后端snake_case不匹配 |
| 附件 GET 返回空值 | 未解决 | schemas.py 序列化问题 |
| Assembly 附件字段 | 未实现 | models + schemas + DB迁移 |
| _updateLocalId BOM引用更新 | 未实现 | 三处联动修改（Store.add/_execSync/_updateLocalId）|

---

## 三、待开发任务清单

### 高优先级

1. **验证 onclick 行导航 Bug**：刷新 http://localhost，打开 F12 Console，点击表格行查看错误信息，精确定位
2. **附件独立存储迁移**（后端已完成，前端 `uploadAttachment` API 调用缺失）

### 中优先级

3. `_editComp done()` currentPart 修复（附件删除 bug）
4. `assembly_version` 版本管理（升版 A→B→C）
5. 前端 sourceFile → source_file 字段统一

### 低优先级（架构问题）

6. `_updateLocalId` BOM引用三处联动修改
7. 字典表同步冲突（架构级）

---

## 四、今日经验教训

1. **PowerShell 编码陷阱**：GBK 文件用 `Get-Content` 读取再写回会损坏中文，改用 Python 二进制模式
2. **text-file-writer 技能强制入口**：所有文本文件写入必须通过 Python 脚本，不得使用内置 write 工具
3. **备份策略**：昨晚直接覆盖 `index.html` 导致今天的部分工作内容丢失（虽然 Git HEAD 保留了今天改的正确部分）
4. **Git 提交时机**：当天完成的工作应及时 `git commit`，避免版本丢失

---

## 五、明日计划

1. **首要**：在浏览器验证 onclick 行导航 bug，定位具体错误
2. **其次**：实现 `uploadAttachment` 独立 API 调用（附件后端已通，前端需补上传调用）
3. **继续**：完善 `_filterPartList` 和 `doReverse` 的 UI 细节
4. **Git**：养成每天收工前 `git commit` 的习惯
