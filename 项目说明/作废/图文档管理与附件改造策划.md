# 图文档管理与零部件附件改造 策划方案

## 一、背景

当前零部件的附件是固定的4个字段（源文件/图纸/STP/PDF），硬编码在 Part 和 Assembly 模型中，无法灵活扩展。新方案将附件提升为独立的一等实体——**图文档**，零部件附件改为引用关联方式，支持用户自行决定关联数量与类别。

## 二、数据库设计

### 2.1 `documents` 表（图文档主表）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | UUID PK | 前端生成 UUID，前后端保持一致 |
| code | String(64) NOT NULL | 用户自定义图文档编号 |
| name | String(255) NOT NULL | 文档名称 |
| doc_type | String(64) | 文档分类，来源→系统设置中的图文档分类字典 |
| version | String(32) default="V1.0" | 版本号 |
| description | Text | 描述 |
| file_name | String(255) | 主附件文件名称（冗余字段，列表显示用） |
| file_id | UUID FK→document_attachments.id ON DELETE SET NULL | 关联的主附件 UUID |
| created_at | DateTime | |
| updated_at | DateTime | |

**唯一约束**：`(code, version)`，与零部件件号+版本一致。

### 2.2 `document_attachments` 表（图文档独立附件表）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | UUID PK | 前端生成 UUID |
| document_id | UUID FK→documents.id ON DELETE CASCADE | 所属图文档 |
| file_name | String(255) | 文件名（含扩展名） |
| file_data | LargeBinary | 文件二进制数据 |
| file_size | Integer | 文件大小（字节） |
| created_at | DateTime | |

**说明**：与现有 `attachments` 表完全独立。新增的图文档使用自己的附件表，互不干扰。

### 2.3 `entity_documents` 表（零部件↔图文档关联表）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | UUID PK | 前端生成 UUID |
| entity_type | String(32) NOT NULL | 实体类型：`part` / `component` |
| entity_id | UUID NOT NULL | 零部件 ID |
| document_id | UUID FK→documents.id ON DELETE RESTRICT | 关联的图文档 ID |
| category | String(64) | 用户自定义类别标签，如"图纸"/"说明书"/"工艺文件"等 |
| sort_order | Integer | 排序号 |
| created_at | DateTime | |

### 2.4 图文档分类字典

在现有 `dictionary` 表中增加 `doc_type` 字典类型，用户在系统设置中管理图文档分类（如：图纸/模型/说明书/图片/其他等）。

### 2.5 清理旧字段

Part 和 Assembly 模型删除以下8个字段及其外键：
- `source_file` (String)
- `source_file_id` (UUID, FK→attachments.id)
- `drawing` (String)
- `drawing_id` (UUID, FK→attachments.id)
- `stp` (String)
- `stp_id` (UUID, FK→attachments.id)
- `pdf` (String)
- `pdf_id` (UUID, FK→attachments.id)

对应的 SQL：`ALTER TABLE parts DROP COLUMN ...`，同步清理 `attachments` 表中不再被引用的孤数据。

## 三、后端 API 设计

### 3.1 图文档管理 API（新路由 `routers/documents.py`）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/documents/` | 列表（分页+搜索，按编号/名称/分类筛选） |
| POST | `/api/documents/` | 创建图文档（前端生成 UUID，body 携带 id） |
| GET | `/api/documents/{id}` | 详情（不含文件数据） |
| PUT | `/api/documents/{id}` | 更新元数据 |
| DELETE | `/api/documents/{id}` | 删除（如有零部件引用则阻止） |

### 3.2 图文档附件 API（在 `routers/documents.py` 中扩展）

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/documents/{id}/attachments` | 上传附件（前端生成 UUID，body 携带 id） |
| GET | `/api/documents/{id}/attachments/{att_id}` | 下载附件（返回文件数据） |
| DELETE | `/api/documents/{id}/attachments/{att_id}` | 删除附件 |
| GET | `/api/documents/{id}/attachments/` | 列出附件列表（不含文件数据） |

上传/替换主附件时，同时更新 documents 表的 `file_name` 和 `file_id`。

### 3.3 实体-文档关联 API（在 `routers/parts.py` / `routers/assemblies.py` 中扩展）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/parts/{id}/documents` | 获取零件的关联图文档列表 |
| POST | `/api/parts/{id}/documents` | 关联图文档（body: document_id, category, sort_order） |
| PUT | `/api/parts/{id}/documents/{edoc_id}` | 更新关联（修改 category/排序） |
| DELETE | `/api/parts/{id}/documents/{edoc_id}` | 移除关联 |

部件同理：`/api/assemblies/{id}/documents`

## 四、前端设计

### 4.1 导航栏增加"图文档管理"

在 `index.html` 侧边栏中 `BOM管理` 后增加：

```html
<a data-page="documents"><span class="nav-icon">📄</span><span>图文档管理</span></a>
```

在 `router.js` 中新增 `documents` 路由 → `Documents.render(c)`。

**新文件**：`frontend/js/pages-documents.js`

### 4.2 图文档管理页面

- **列表页**：表格展示（编号、名称、分类、版本、文件名称、更新时间），支持搜索筛选
- **创建**：弹窗表单（编号、名称、分类下拉、版本、描述），UUID 前端生成
- **详情/编辑**：上方元数据表单，下方附件区域
- **附件上传**：点击"上传附件"→ 选择文件 → 前端生成 UUID → 上传到后端 → 更新 `file_name`/`file_id`
- **附件列表**：显示文件名、大小、上传时间，提供[下载][删除]按钮
- **删除图文档**：有关联引用时提示"已被 XX 个零部件引用，无法删除"

### 4.3 零部件附件改造

**编辑弹窗改造（pages-parts.js / pages-components.js）：**
- 移除原有的4个固定附件行
- 新增"关联图文档"区域，动态列表
- 每行：类别标签（自由输入文本）+ 图文档编号·名称 + [选择] [移除]
- 点击[选择] → 弹出图文档选择器弹窗（搜索框+列表），选中后填写类别标签
- 底部[添加关联]按钮，可添加任意多个

**详情页改造：**
- 附件区域改为动态列表，按类别分组显示

### 4.4 系统设置扩展

`pages-settings.js` 中的字典管理增加 `doc_type` 分类，用户可增删改图文档分类选项。

### 4.5 UUID 一致策略

- 前端 `_uuid()` 生成 UUID v4
- 创建图文档/上传附件时，API body 中携带 `id` 字段
- 后端使用该 `id` 创建记录（与当前 Attachment 的 `id` 处理逻辑一致）

### 4.6 同步机制

图文档使用实时同步（每次操作直接调 API），不做本地离线缓存。`store.js` 中不新增 `documents` 集合，但 `entity_documents` 关联数据随 Part/Component 同步纳入本地存储。

## 五、数据流示意

```
场景：创建一个钣金件零件，关联一份图纸图文档

1. 进入"图文档管理"
   → 上传图纸文件（前端生成 UUID）
   → 后端记录到 document_attachments 表，更新 documents.file_id/file_name

2. 进入"零件管理"，创建/编辑零件
   → 在"关联图文档"区域点击[选择]
   → 弹出图文档选择器，搜索选取刚创建的图文档
   → 输入类别标签：如"图纸"

3. 保存零件
   → entity_documents 表写入：
     entity_type='part', entity_id=xxx, document_id=yyy, category='图纸'
```

## 六、实施步骤

### 阶段一：基础框架（1-2天）

1. 创建 `documents` + `document_attachments` + `entity_documents` 表模型
2. 删除 Part/Assembly 旧附件字段
3. 实现图文档 CRUD API + 附件上传/下载 API
4. 实现图文档前端页面（列表+创建+附件管理）

### 阶段二：关联改造（2-3天）

5. 在 Parts/Components API 中扩展关联接口
6. 改造编辑弹窗关联图文档区域（动态列表+选择器）
7. 改造详情页附件显示
8. 系统设置增加图文档分类字典

### 阶段三：清理（1天）

9. 删除旧附件相关的前端代码（4个固定字段的渲染/上传/下载/删除逻辑）
10. 清理数据库中旧的 `attachments` 表未被引用的数据
11. 端到端测试

---

## 七、大文件上传改造方案

### 7.1 背景与目标

**当前问题**：
1. 文件数据通过 Base64 编码传输，增加 33% 的数据大小
2. 大文件存储在数据库中（LargeBinary/BYTEA），影响数据库性能
3. 数据库备份和恢复变慢
4. 内存占用高（整个文件需要加载到内存中）
5. 无断点续传支持，大文件上传失败需要重新上传

**目标**：
1. 使用文件系统存储，不占用数据库空间
2. 支持大文件分块上传，提高上传稳定性
3. 支持断点续传
4. 提供上传进度显示
5. 保持向后兼容

### 7.2 技术方案设计

#### 7.2.1 存储方式

**文件系统存储**：
- 文件存储在服务器文件系统中
- 数据库只存储文件路径、大小、哈希值等元数据
- 按实体类型和 ID 组织目录结构

**目录结构**：
```
uploads/
├── documents/
│   └── {document_id}/
│       └── {filename}
├── parts/
│   └── {part_id}/
│       └── {filename}
├── assemblies/
│   └── {assembly_id}/
│       └── {filename}
└── chunks/
    ├── {upload_id}.meta
    ├── {upload_id}_0.chunk
    ├── {upload_id}_1.chunk
    └── ...
```

#### 7.2.2 分块上传流程

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  1. 初始化   │────▶│  2. 上传分块 │────▶│  3. 合并完成 │
└─────────────┘     └─────────────┘     └─────────────┘
       │                   │                   │
       ▼                   ▼                   ▼
  生成 upload_id     上传每个分块         合并所有分块
  计算总块数         更新进度             保存到文件系统
  创建元数据         支持断点续传          清理临时文件
```

#### 7.2.3 配置参数

| 参数 | 默认值 | 说明 |
|-----|--------|------|
| UPLOAD_DIR | ./uploads | 文件存储根目录 |
| CHUNK_DIR | ./uploads/chunks | 分块临时目录 |
| MAX_FILE_SIZE | 100MB | 最大文件大小限制 |
| CHUNK_SIZE | 5MB | 每个分块的大小 |

### 7.3 数据库设计

#### 7.3.1 扩展 `document_attachments` 表

| 字段 | 类型 | 说明 |
|---|---|---|
| id | UUID PK | |
| document_id | UUID FK | 所属图文档 |
| file_name | String(255) | 文件名 |
| file_data | LargeBinary | 文件数据（保留用于向后兼容） |
| file_size | Integer | 文件大小（字节） |
| **file_path** | **String(512)** | **新增：文件系统路径** |
| **file_hash** | **String(64)** | **新增：文件 SHA256 哈希值** |
| created_at | DateTime | |

**迁移 SQL**：
```sql
ALTER TABLE document_attachments ADD COLUMN IF NOT EXISTS file_path VARCHAR(512);
ALTER TABLE document_attachments ADD COLUMN IF NOT EXISTS file_hash VARCHAR(64);
CREATE INDEX idx_document_attachments_file_path ON document_attachments(file_path);
CREATE INDEX idx_document_attachments_file_hash ON document_attachments(file_hash);
```

### 7.4 后端 API 设计

#### 7.4.1 文件存储服务类

**文件**：`backend/app/file_storage.py`

**类设计**：

```python
class FileStorage:
    """文件系统存储服务"""
    
    def save_file(file_data, entity_type, entity_id, filename) -> dict:
        """保存文件到文件系统"""
        # 1. 检查文件大小
        # 2. 计算文件哈希
        # 3. 保存到文件系统
        # 4. 返回文件信息
    
    def read_file(file_path) -> bytes:
        """读取文件"""
    
    def delete_file(file_path) -> bool:
        """删除文件"""
    
    def get_file_info(file_path) -> dict:
        """获取文件信息"""

class ChunkedUploader:
    """分块上传管理器"""
    
    def init_upload(filename, file_size, entity_type, entity_id, total_chunks) -> dict:
        """初始化分块上传"""
        # 1. 生成 upload_id
        # 2. 创建元数据文件
        # 3. 返回上传信息
    
    def upload_chunk(upload_id, chunk_index, chunk_data) -> dict:
        """上传分块"""
        # 1. 读取元数据
        # 2. 保存分块文件
        # 3. 更新元数据
        # 4. 返回进度信息
    
    def complete_upload(upload_id) -> dict:
        """完成上传，合并所有分块"""
        # 1. 检查所有分块是否上传
        # 2. 合并分块
        # 3. 保存到文件系统
        # 4. 清理临时文件
        # 5. 返回文件信息
    
    def get_upload_status(upload_id) -> dict:
        """获取上传状态"""
    
    def cancel_upload(upload_id) -> bool:
        """取消上传，清理临时文件"""
```

#### 7.4.2 新版附件 API

**路由**：`/api/v2/attachments`

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | /upload | 直接上传文件（小文件） |
| POST | /chunk/init | 初始化分块上传 |
| POST | /chunk/upload | 上传分块 |
| POST | /chunk/complete | 完成上传 |
| GET | /chunk/status/{upload_id} | 获取上传状态 |
| DELETE | /chunk/cancel/{upload_id} | 取消上传 |
| GET | /{attachment_id} | 获取附件信息 |
| GET | /{attachment_id}/download | 下载附件 |
| DELETE | /{attachment_id} | 删除附件 |

**API 详细设计**：

**1. 直接上传（小文件）**
```
POST /api/v2/attachments/upload
Content-Type: multipart/form-data

参数：
- file: 文件
- entity_type: 实体类型 (document/part/assembly)
- entity_id: 实体ID

返回：
{
  "id": "uuid",
  "file_name": "example.pdf",
  "file_size": 12345,
  "file_path": "documents/uuid/example.pdf",
  "message": "文件上传成功"
}
```

**2. 初始化分块上传**
```
POST /api/v2/attachments/chunk/init
Content-Type: application/x-www-form-urlencoded

参数：
- filename: 文件名
- file_size: 文件总大小
- entity_type: 实体类型
- entity_id: 实体ID

返回：
{
  "upload_id": "uuid",
  "total_chunks": 10,
  "chunk_size": 5242880,
  "message": "分块上传初始化成功"
}
```

**3. 上传分块**
```
POST /api/v2/attachments/chunk/upload
Content-Type: multipart/form-data

参数：
- upload_id: 上传ID
- chunk_index: 分块索引（从0开始）
- chunk: 分块数据

返回：
{
  "upload_id": "uuid",
  "chunk_index": 0,
  "uploaded_chunks": [0, 1, 2],
  "total_chunks": 10,
  "progress": 30.0,
  "is_complete": false,
  "message": "分块 0 上传成功"
}
```

**4. 完成上传**
```
POST /api/v2/attachments/chunk/complete
Content-Type: application/x-www-form-urlencoded

参数：
- upload_id: 上传ID

返回：
{
  "id": "uuid",
  "file_name": "example.pdf",
  "file_size": 12345678,
  "file_path": "documents/uuid/example.pdf",
  "status": "completed",
  "message": "文件上传完成"
}
```

### 7.5 前端设计

#### 7.5.1 文件上传工具类

**文件**：`frontend/js/file-uploader.js`

```javascript
class FileUploader {
  constructor(options = {}) {
    this.chunkSize = options.chunkSize || 5 * 1024 * 1024;  // 5MB
    this.maxFileSize = options.maxFileSize || 100 * 1024 * 1024;  // 100MB
    this.onProgress = options.onProgress || (() => {});
    this.onComplete = options.onComplete || (() => {});
    this.onError = options.onError || (() => {});
  }

  async upload(file, entityType, entityId) {
    // 自动选择直接上传或分块上传
    if (file.size <= this.chunkSize * 2) {
      return await this._directUpload(file, entityType, entityId);
    } else {
      return await this._chunkedUpload(file, entityType, entityId);
    }
  }

  async _directUpload(file, entityType, entityId) {
    // 直接上传小文件
  }

  async _chunkedUpload(file, entityType, entityId) {
    // 1. 初始化上传
    // 2. 上传所有分块
    // 3. 完成上传
  }

  async getStatus(uploadId) {
    // 获取上传状态
  }

  async cancel(uploadId) {
    // 取消上传
  }
}

// 便捷方法
async function uploadFile(file, entityType, entityId, options = {}) {
  const uploader = new FileUploader(options);
  return await uploader.upload(file, entityType, entityId);
}

async function downloadFile(attachmentId) {
  // 下载文件
}
```

#### 7.5.2 使用示例

**基本使用**：
```javascript
// 上传文件
const result = await uploadFile(file, 'document', documentId, {
  onProgress: (progress) => {
    console.log(`上传进度: ${progress.progress}%`);
    console.log(`已上传: ${progress.uploaded_chunks.length}/${progress.total_chunks} 块`);
  },
  onComplete: (result) => {
    console.log('上传完成:', result);
  },
  onError: (error) => {
    console.error('上传失败:', error);
  }
});
```

**带进度条的上传**：
```javascript
// 创建上传器实例
const uploader = new FileUploader({
  chunkSize: 5 * 1024 * 1024,  // 5MB
  maxFileSize: 100 * 1024 * 1024,  // 100MB
  onProgress: (progress) => {
    // 更新进度条
    progressBar.style.width = `${progress.progress}%`;
    progressText.textContent = `${progress.progress.toFixed(1)}%`;
  }
});

// 上传文件
const result = await uploader.upload(file, 'document', documentId);
```

### 7.6 Docker 配置

#### 7.6.1 docker-compose.yml 配置

```yaml
backend:
  build:
    context: ./backend
    dockerfile: Dockerfile
  environment:
    - UPLOAD_DIR=/app/uploads
    - CHUNK_DIR=/app/uploads/chunks
    - MAX_FILE_SIZE=104857600
    - CHUNK_SIZE=5242880
  volumes:
    - ./uploads:/app/uploads
```

#### 7.6.2 上传目录创建

```bash
mkdir -p uploads/documents uploads/chunks uploads/parts uploads/assemblies
chmod 755 uploads uploads/documents uploads/chunks uploads/parts uploads/assemblies
```

### 7.7 实施步骤

#### 阶段一：后端改造（1天）

1. 创建文件存储服务类 `file_storage.py`
2. 创建新版附件路由 `attachments_v2.py`
3. 扩展数据库模型，添加 `file_path` 和 `file_hash` 字段
4. 执行数据库迁移
5. 更新 `main.py`，注册新版 API 路由
6. 添加依赖到 `requirements.txt`

#### 阶段二：前端改造（1天）

7. 创建文件上传工具类 `file-uploader.js`
8. 在 `index.html` 中引入 `file-uploader.js`
9. 修改现有文件上传逻辑，使用新的 API

#### 阶段三：部署与测试（0.5天）

10. 创建上传目录
11. 更新 Docker 配置
12. 重启后端服务
13. 测试文件上传功能
14. 测试大文件分块上传
15. 测试断点续传

### 7.8 特性总结

| 特性 | 说明 |
|-----|------|
| ✅ 文件系统存储 | 文件存储在文件系统中，不占用数据库空间 |
| ✅ 分块上传 | 大文件自动分块上传，支持断点续传 |
| ✅ 进度显示 | 实时上传进度回调 |
| ✅ 文件大小限制 | 可配置最大文件大小 |
| ✅ 文件哈希 | SHA256 哈希值用于文件完整性校验 |
| ✅ 向后兼容 | 保留 `file_data` 字段，旧文件仍可访问 |
| ✅ 自动选择 | 小文件直接上传，大文件自动分块上传 |

### 7.9 性能对比

| 指标 | 改造前 | 改造后 |
|-----|--------|--------|
| 传输格式 | Base64（+33% 大小） | 二进制（原始大小） |
| 存储位置 | 数据库 | 文件系统 |
| 最大文件 | ~75MB | 可配置（默认 100MB） |
| 断点续传 | ❌ 不支持 | ✅ 支持 |
| 数据库备份 | 慢（包含文件） | 快（不含文件） |
| 内存占用 | 高（整个文件） | 低（分块处理） |

### 7.10 注意事项

1. **向后兼容**：
   - 保留 `file_data` 字段，旧文件仍可通过数据库访问
   - 新上传的文件使用文件系统存储
   - 可通过迁移脚本将旧文件迁移到文件系统

2. **安全性**：
   - 文件大小限制防止恶意上传
   - 文件哈希校验确保完整性
   - 文件名处理防止路径遍历攻击

3. **清理策略**：
   - 定期清理未完成的分块上传（超过一定时间）
   - 删除附件时同时删除文件系统中的文件

4. **监控**：
   - 监控上传目录的磁盘使用
   - 监控上传成功率和失败率
   - 监控平均上传时间
